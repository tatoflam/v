# 2026-07-15

## 10:16  手動フル運用一気通貫（wiki catch-up ツール新設 + きつね塚誤検知是正 + republish）
- session: 20544af1  cwd: MeguruPMReport
- 「フル手動実行して」を手動フルパイプラインの合図フレーズに固定（順序: wiki更新→全リソース差分収集→週次生成→writeback→Milestone再取得+グリッド→publish（最後）→alert）。
- wiki 最終 sync が 6/16 で止まっていたため、再利用可能な catch-up runbook **`scripts/wiki_catchup.sh`** を新設（可変ルックバック・カーソル管理の差分取得・`--model`/`--since`、firehose 用 checkpoint モード、端末実行必須 + `WIKI_INGEST_WORKER=1` ガード）。`--model opus --since 2026-06-16` で実行。
- きつね塚通り前の writeback 誤 TRUE 9 セル（重複 Jooto ボード 1294084 の stale done 由来）を正ボード 1315261 基準で FALSE 戻し、幡ヶ谷不動尊通りは構造着手→仮受付に正当前進 → 修正版を HTML/Doc/Markdown へ republish。
- wiki 取得の Gmail 読み（Step1・design-log 宛・2日窓・.eml 保存）と週次レポートの Gmail ライブ読み（Step3・案件ラベル・8日窓・読むだけ）は「別レンズの 2 読み手」で重複ではない、と整理して合意。
- see also: [[03_work/meguru-pm-report]] · [[03_work/meguru-wiki]]

## 12:31  7/15 週次成果を commit + push で確定
- session: 20544af1  cwd: MeguruPMReport
- 週次実行＋runbook の成果物を 1 コミットにまとめて push（commit `29284f9`, branch `feat/weekly-anchor-and-wiki-drilldown`, 6 files / +1058 / -550）。
- 中身: `reports/2026-07-15_weekly_update.md`（新規・27 案件確定版）+ `scripts/wiki_catchup.sh` + baseline 7/15 更新（`state/latest_*`）+ 7/10 レポートの Doc 最新版置換。PR マージ/ブランチ削除はパイロット待ちで保留。
- see also: [[03_work/meguru-pm-report]]

## 20:30  昨夜の Wiki Absorb 全滅を原因特定・修正・約1ヶ月分リカバリ
- session: 129723c3  cwd: MeguruPMReport
- 昨夜の一括 absorb（79 shard/6,509 件）が commit ゼロで見かけ上完走 → 全記事 mtime が seed 日のまま＝1件も書けていなかった。真因は `claude -p` を cwd 未指定で起動し `shards/…` 相対パスが解決不能で無編集 exit 0（+ `--add-dir` 直後の prompt 食い）。`absorb_common.py` を cwd=wiki/stdin/絶対パス curator/acceptEdits/パス限定 staging に修正（27 テスト green, `Plugins-Meguru@717a23e`）。
- seed 後 1,541 件（63 shard）を並列 4・nohup で absorb、失敗ゼロ、MeguruWiki に 64 per-shard commit。3 リポ push 済み。
- see also: [[03_work/business-curator]] · [[03_work/meguru-wiki]]

## 20:34  jooto.last_synced 恒久対策 change を起票→apply→archive→PR マージ
- session: 129723c3  cwd: MeguruPMReport
- wiki の last_synced が過去日固定なのは、absorb が本文しか触らず `jooto.*` frontmatter を更新する経路が無いため（seed 時 4/14・6/16 のまま）。恒久対策として `add-jooto-wiki-frontmatter-sync` を起票し apply（17/17, strict valid）。新ツール `jooto_frontmatter_sync.py` は `board.json` を正に `jooto:` ブロック 2 行のみ in-place 置換（本文バイト等価保全）、daily Step 1b に非致命配線。
- 初回同期で 32 案件更新（各1行のみ）→ PR #4 で main 反映。判明した運用制約: 実行機に yq 未導入のため config ゲートが効かず、Step 1b 自動化には `brew install yq` が前提。
- see also: [[03_work/meguru-pm-report]] · [[03_work/business-curator]]

## 20:36  ThreadsPosts を 10 分野自律スケールアウトへ + wave 1 アカ otoriyosememo 起票
- session: 8aec1ae2  cwd: ThreadsPosts
- OpenSpec `ten-genre-autonomous-scaleout` を起票→apply（53/54, npm test 771 green）。dept/ を genres/<G>/accounts/<A> 二層へ git mv、GENRE 環境変数を廃止し account→genre 自動解決、10 投稿/日ハードキャップ、publish/loop の全アカ fan-out、launchd 無人化 3 job、管理者 runbook（週次 15-30 分の極小運用）。旧 2 change を吸収。
- マーケットリサーチ: 楽天は 1 商品 ¥1,000 報酬上限のため中単価 ¥2-8k リピート型が有利。wave 1 = お取り寄せ + 観葉植物。分離ルールは eSIM/回線を外しアカウント 5 軸のみに緩和。
- wave 1 の 1 アカ目 `otoriyosememo`（お取り寄せグルメ）を調達完了（handle/user_id/Meta app/token/楽天アフィリ ID）+ persona 初版。voice 人ゲート承認待ち。
- see also: [[03_work/threadsposts]]
