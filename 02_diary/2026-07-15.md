# 2026-07-15

## 10:16  手動フル運用一気通貫（wiki catch-up ツール新設 + きつね塚誤検知是正 + republish）
- session: 20544af1  cwd: MeguruPMReport
- 「フル手動実行して」を手動フルパイプラインの合図フレーズに固定（順序: wiki更新→全リソース差分収集→週次生成→writeback→Milestone再取得+グリッド→publish（最後）→alert）。
- wiki 最終 sync が 6/16 で止まっていたため、再利用可能な catch-up runbook **`scripts/wiki_catchup.sh`** を新設（可変ルックバック・カーソル管理の差分取得・`--model`/`--since`、firehose 用 checkpoint モード、端末実行必須 + `WIKI_INGEST_WORKER=1` ガード）。`--model opus --since 2026-06-16` で実行。
- きつね塚通り前の writeback 誤 TRUE 9 セル（重複 Jooto ボード 1294084 の stale done 由来）を正ボード 1315261 基準で FALSE 戻し、幡ヶ谷不動尊通りは構造着手→仮受付に正当前進 → 修正版を HTML/Doc/Markdown へ republish。
- wiki 取得の Gmail 読み（Step1・design-log 宛・2日窓・.eml 保存）と週次レポートの Gmail ライブ読み（Step3・案件ラベル・8日窓・読むだけ）は「別レンズの 2 読み手」で重複ではない、と整理して合意。
- see also: [[03_work/meguru-pm-report]] · [[03_work/meguru-wiki]]

## 12:31  7/15 週次成果を commit + push で確定
- session: 20544af1  cwd: MeguruPMReport
- 週次実行＋runbook の成果物を 1 コミットにまとめて push（commit `29284f9`, branch `feat/weekly-anchor-and-wiki-drilldown`, 6 files / +1058 / -550）。
- 中身: `reports/2026-07-15_weekly_update.md`（新規・27 案件確定版）+ `scripts/wiki_catchup.sh` + baseline 7/15 更新（`state/latest_*`）+ 7/10 レポートの Doc 最新版置換。PR マージ/ブランチ削除はパイロット待ちで保留。
- see also: [[03_work/meguru-pm-report]]
