# 2026-07-10

## 08:27  MeguruPMReport プラグイン symlink 統合を本番検証 + 07-08 週次 publish + Phase C 完了
- session: 3bb3ec29  cwd: MeguruPMReport
- consolidate-grabber-plugins **Phase B**: `plugins/*` を `meguruit/Plugins-Meguru` への相対 symlink に置換（ランタイム非破壊コピー、全6プラグイン520テストgreen、`install_plugin_symlinks.sh` で可逆化）。「安全な方法で」の指示に従った read-only 実API疎通確認が、**明朝の日次パイプラインを壊す重大バグを事前検出**：symlink 経由だと `__file__`/`cwd` が Plugins-Meguru に解決され共有プラグインが消費側リポを導出不能 → `MEGURU_PM_REPO_ROOT` env 方式で修正（daily_pipeline が export）
- **2026-07-08 週次レポートを生成・publish**（27案件、diff vs 07-05、東十条3丁目が5マイルストーン前進）。この実API取得が Phase B の本番検証を兼ねた。配信 <https://meguru-pm-report.web.app/r/2026-07-08/> / Google Doc
- publish 時に判明：**firebase-tools 15.x が `--token` を廃止**（FIREBASE_TOKEN が失効対話認証に黙ってフォールバック）→ この回は `firebase login --reauth` で回避、恒久はサービスアカウント方式が必要。**Phase C** も完了（`data/`→`~/meguru-data` + `MEGURU_DATA_ROOT`、credential を `~/.config/meguru-plugins/` 集約）+ OpenSpec archive
- see also: [[03_work/meguru-pm-report]], [[06_output/2026-07]]

## 08:55  MeguruPMReport 2026-07-10 週次レポートをフルセット生成・書き戻し・配信 + Firebase デプロイを .env トークン化
- session: 1265d8ce  cwd: MeguruPMReport
- **フルセット週次（9 工程、run_date=2026-07-10 / diff vs 07-08）**を順序厳守で実行: Jooto backup(52 ボード)→期日超過(61 件、案件対象は東長崎4丁目 省エネ申請のみ横ばい)→PM マスタ取得→baseline sync + carry-forward(保持 2 件)→Gmail 差分収集(27 案件・5 並列)→レポート生成→Milestone 書き戻し(12 セル全 FALSE→TRUE)→再取得+工程グリッド再生成→publish。**工程軸が 10 案件前進**（Milestone 行整備で 5 案件が進捗未設定→平面検証、書き戻し同期で 5 案件前進: 堀ノ内2丁目→構造KO・高田馬場→構造着手・江古田江原→仮受付・山王3丁目→本受付・東十条3丁目→設備検討）。意匠負荷は井上 4 件・品田 3 件に逼迫
- 配信 <https://meguru-pm-report.web.app/r/2026-07-10/> / Google Doc（16_2qkRnNM2xTBIqZTav_0lauGBsqQGuuPNafch5oK-o）
- **Firebase デプロイを .env 由来 `FIREBASE_TOKEN` で非対話化**（初回 publish が認証失効で deploy 失敗したのが動機）。`publish.py` に `.env` ローダ追加・`.env.example` 作成、OpenSpec `add-firebase-deploy-token` を起票→validate→archive、両リポ commit + push。これで launchd 日次から `firebase login` 再認証なしにデプロイ可。CI トークン方式は様子見（非推奨化中、頻繁失効ならサービスアカウント移行）
- see also: [[03_work/meguru-pm-report]], [[06_output/2026-07]]

## 18:30  ToDoBot 通知先を更新（1課・2課から mashikaya を削除）してデプロイ
- session: ad4dd117  cwd: ToDoBot
- `config/profiles.yaml` の 1課・2課 `email_to` から `mashikaya@meguru-construction.com` を削除。`load_profiles` 検証 + `pytest` 187件green の上で `scripts/deploy.sh functions` を実行、4関数（asia-northeast1）を更新
- 通知先変更の反映手順を確認：`profiles.yaml` は Firestore でなく **関数バンドル同梱**（predeploy が `functions/config/` へコピー）→ 編集して再デプロイするだけ、Firestore書込やsecrets:setは不要
- see also: [[03_work/todobot]]
