# 2026-07-09

## 14:29  みなみ学童Tシャツ 公開サイトの募集締切を 2026-07-05 に更新
- session: 63f673e5  cwd: minami-gakudo-tshirt
- 公開サイトの締切を 2026-07-05 に更新し反映を検証（deploy run 28881658207, success 36秒）
- 「表示が更新されない」原因を特定：締切の真実は GitHub Actions Secret `DEADLINE` → build-config.js → Cloudflare Pages で、手元の `config.js`（gitignore）はローカルプレビュー専用だった
- backend（自動返信メールの受渡時期文言）は汎用「募集締切から約1ヶ月」のままで合意。clasp run はスコープ／GCP紐付けの都合で今回も使えず
- see also: [[04_life/minami-gakudo-tshirt-site]]

## 16:18  MeguruInvoice 企画一式（OpenSpec propose ＋ 提案書 ＋ Firebase改訂 ＋ GitHub化）
- session: 4fa71e7f  cwd: MeguruInvoice
- 取引先請求書PDF（手書き含む）を Drive から一括取込→レビュー→スプレッドシート反映する社内Webシステムの企画。Fable が OpenSpec `add-invoice-intake`（proposal/design/specs×5/tasks×42/handoff）を作成し、Opus apply 用の CLAUDE.md/handoff.md で引き継ぎを明文化
- フィードバック反映で大幅改訂: Cloud SQL 廃止→**Firebase App Hosting + Firestore**（運用費 月2,000〜4,000円、旧構成1/4以下）／AI抽出は Gemini 第一候補だが要件定義時に OpenAI と実サンプルPoC比較で確定・抽象化／見積 1人月160h・1h1万円で**664h＝664万円**／本番実務シート（`1XIgM…`）への開発中の直接書込を三箇所でNG明記
- private リポジトリ `meguruit/MeguruInvoice` を新規作成し push（28de165 / c043a37）。Mermaid システムコンテキスト図を docs と提案書HTMLに追加（提案書は claude.ai 非公開・ローカルのみ）
- see also: [[03_work/meguru-invoice]], [[03_work/meguru-facade-studio]]

## 19:29  meguru-construction.com 問い合わせフォームが無言で消える問題を切り分け
- session: 4ed42dae  cwd: website-theme-meguru
- 症状「サンクスページは出るのに問い合わせが届かない・エラーも出ない」の真因を確定：`contact_send.php` が GAS 中継失敗時に `通信エラー` を **HTTP 200** で返し、jQuery が success 扱いで `/thanks/` へ飛ばす「失敗が成功に見える」構造
- 切り分け実測: レスポンス本文=`通信エラー`／TTFB 3.2秒 → GAS 中継到達を確認、GET で GAS デプロイ生存＆アクセス権「全員」を確認、POST で `{"status":"error",...}` を得て **PHP 無罪・GAS の doPost 内部エラー**（握り潰された独自 catch 文言）と特定。user 側は GAS の権限調整＋エラーハンドリングで解決
- 提案（未適用）: 失敗時に 4xx/5xx を返し `.fail()` を発火／`count($_POST)<5` のオフバイワン／`Content-Type` のセミコロン欠落／`file_get_contents` タイムアウト・`allow_url_fopen`・302 再送トラップ
- see also: [[03_work/meguru-website]]

## 13:53  vault から tatoflam プロファイルを生成し 00_self ペルソナを更新
- session: 2a41fc48  cwd: v
- `00_self/` を軸に work/life 実案件で裏付けたプロファイルを提示。氏名の正しい漢字は「**本間 達朗**」（「郎」は誤り）と user 訂正を確認
- `00_self/skills.md` を実証ベースで拡充（TypeScript / AWS Lambda・DynamoDB / Firebase / Next.js・Prisma / Claude・OpenAI + OpenSpec 駆動）、`profile.md` を法人3期目に鮮度更新（commit 93a1612）
- see also: [[00_self/profile]] [[00_self/skills]]

## 14:57  NoteCreator を立ち上げ — note 収益化システムの propose→実装→分野承認まで完走
- session: b044bc44  cwd: NoteCreator
- OpenSpec change note-content-monetization-system を起票し、notecreator CLI（Python）を 32/36 タスク実装、private リポ tatoflam/NoteCreator を新規作成して push
- リサーチ 21 件収集 → 00_self 由来の operator プロフィールで再スコアリングし、分野を ai-katsuyo で承認。voicing profile v1 と企画 3 案（推奨 plan_003）まで生成
- .env config-injection のセキュリティ指摘を修正（読み込みをリポジトリルート限定に）
- see also: [[00_self/profile]]
