# 2026-07-14

## 10:28  consolidate-project-identity を設計→実装→5リポ横断PR→archive まで完結
- session: 9dbc773d  cwd: MeguruPMReport
- projects.csv の FY26 ラベルと MeguruWiki resolver-slug の 2 キー問題を解消する change を起票。実データ突合で「27 案件中 8 件（約30%）が今は slug に単一キー化できない（新案件の shard 未 absorb、Gmail ラベルは 27/27 網羅）」と判明 → **slug を唯一 PK ではなく nullable な join FK に緩める**設計に転換。
- 3 リポ（MeguruPMReport #4/#5/#6・Plugins-Meguru #1・MeguruWiki #1）に PR 作成 → マージ → ブランチ掃除、`/opsx:archive` で specs を `project-identity-registry` に確定。
- 次の着手候補は add-triage-routing-enrichment（0/36、全角/半角吸収・slug 複数候補化もここへ）。
- see also: [[03_work/meguru-pm-report]] · [[project_consolidation_resume]]

## 14:44  add-triage-routing-enrichment を実装→実データ§7検証→3リポPRマージ→archive まで完結
- session: 02e6aaab  cwd: MeguruPMReport
- 5 段 triage パイプライン（Noise→Domain→Category→Alias→LLM）を business-curator に実装。slug-aware LLM fallback は候補 pre-rank + slug 強制で `primary_slug=None` を禁止戻り値化。`_alias_resolver.md` に `## routing/`（domain 28 / noise 6 / category 3）投入。business-curator 673 tests green。
- 当初「6時cronが自然に§7実行」は**誤りと判明**（cron未登録・メール0件・_env.sh旧参照）→ `_env.sh` repoint + Gmail ingest で**実メール5,716通を取り込み再triage（noise 2→112）**、分析母集団を togami-log@ ∪ design@ 宛 2,249件に確定。
- PII eml 5,717件(2.5GB)を揮発する `/tmp` から git-safe な `~/meguru-data/mail/thomma/` へ恒久移動。
- 実装3PR（Plugins-Meguru#2 / MeguruWiki#2 / MeguruPMReport#7）マージ → `openspec archive` で新capability `triage-classifier`（6 req）を specs 確定（15/15 green）→ archive PR#8。
- see also: [[03_work/meguru-pm-report]] · [[03_work/meguru-wiki]] · [[project_consolidation_resume]]

## 21:14  docs整備→fix-mbox→wiki-absorbツール→sync→archive2件→daily_pipeline §2配線を6PRで完結
- session: 0faed776  cwd: MeguruPMReport
- 長丁場（約6.5h）。docs一式＋システムコンテクスト図(mermaid)整備 → `fix-mbox-parser-encoding`（新capability `mail-ingest`、get_bytes＋per-message recovery）→ business-curator の wiki absorb ツール3種（35 tests）→ sync ツール → `align-daily-pipeline-spec-with-cli-execution`（base specを「6ステップ・CLI直呼び」に是正）を archive。
- `daily_pipeline.sh` に §2 配線（wiki_refresh Step 0a–0c ＋ Step 7 auto-absorb ＋ 月曜catch-up）を追加。**全フラグ default OFF・非致命** → マージしても runtime挙動は不変、有効化は1日ピロット較正後のops判断。
- アーキ選択は **a案（`~/meguru-data` にデータ集約）で確定**。母集団は「定例レポートを除き意味のあるメールは全量解析」方針、フィルタは Step 0a gmail-grabber `--raw-query` で適用。
- **TDD/実測で3件の前提誤りを commit前に是正**（fix-mbox破損仮説 / sync入力 / `ingest_cli`にto-ccフィルタ無し）。
- 6PR（MeguruPMReport #9/#11/#12・Plugins-Meguru #3/#4 ほか）全マージ、ローカルmainのみに掃除。`add-wiki-absorb-orchestration` 24/50、open change残り2本（wiki-absorb継続分 / weekly-anchor 0/26 水曜アンカー差分をpropose）。
- see also: [[03_work/meguru-pm-report]] · [[03_work/businesscurator]] · [[project_weekly_report_daily_anchor_diff]]
