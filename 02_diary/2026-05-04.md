---
title: 2026-05-04
category: 02_diary
tags: [diary]
sources: [ce4cb7d1-c726-49a8-9b98-b1f7c1856063, 57a002bd-6c29-47d4-ae0b-f42b43b5b03d, 34378f6f-2063-4374-a4c3-038459b80765, 930584ad-9c03-49da-8b8a-2e72611b4093, d40649e2-fb8b-4e0a-8c18-14bc3a972ea8, fdd9f971-ba48-4587-8f3b-695edf0453ea, a73c0aa2-c9a4-46e3-ab60-72b6b426901a, e723638b-4cc6-4772-a855-6d8350a30071, 8b57f7c8-b8fa-4f65-8c06-06cf6fbe87b3, 7a941d5c-720e-44f5-b18e-71bf4b5a9c42, 97d3f618-8d6c-40e6-8210-06549512f183, 2efd7524-d0be-4d70-b0f6-f252268ab156, cef7a3c1-5798-4534-ab51-63c1a2279734, c02fca64-85c4-40f5-9bae-00ea56f138c1]
updated: 2026-05-04
---

# 2026-05-04

## research-pipeline-k006 完走 — keyword/URL → K### Knowledge 自動生成パイプラインを 1 セッションで起票→実装→PR→archive（ce4cb7d1、5-3 24:26 → 5-4 03:02 JST）

- session: ce4cb7d1  cwd: ThreadsPosts
- 起点: 5-3 evening の e01596df セッション末尾で「会社化（cc-company B+C ハイブリッド）方針 + Drafts 在庫補充の必要性（5/9 以降ゼロ）」が宿題化していた。**user 提案で会社化提案そのものより先に、「キーワードを入力すると YouTube 検索 → 文字起こし → Topics 生成 → K### に保存」する research パイプラインを優先**。CLAUDE.md にも記載済の 6 部署のうち `dept/research/` は実装ゼロだったため、ここを起点にする
- 結果: **62 タスク完走、PR #2 マージ、K006/K007/K008 生成、archive commit `712ac50` まで 1 セッションで貫通**。OpenSpec capability 7→8（`research-pipeline` 新設）

### Phase A — `/opsx:propose research-pipeline-k006` 起票（4 artifacts）

`openspec/changes/research-pipeline-k006/` に proposal / design / specs / tasks 一式（4/4 validate）。design.md の D1-D10 で:

- CLI 構成: `npm run research -- --keyword <kw>` / `--url <youtube-url>`、`--max-candidates` / `--auto-pick` / `--force` / `--dry-run`
- D2 候補選択: YouTube Data API v3 直叩き、再生数×新しさで top N
- **D3 字幕取得（後で全面書換え）**: 当初は `youtube-transcript` (npm pkg) で timedtext 一択、字幕なし動画は skip。ASR は不採用と仮置き
- D5 Topics 合成: Claude Sonnet 4.6 + プロンプトキャッシュ + K005 を few-shot
- D6 frontmatter: `topic_id` / `source_url` / `generated_at` / `model` / `transcript_path` / `status: draft_auto` 必須
- D7 採番: K001-K005 は legacy frontmatter なし、最大連番をディレクトリスキャンで取得し +1
- D8 dedup: `source_url` の URL 正規化（v= パラメータのみ抽出）で既存 transcript と衝突検出
- D9 env: `YOUTUBE_API_KEY` / `ANTHROPIC_API_KEY` 必須、`OPENAI_API_KEY` はオプション

### Phase B — `/opsx:explore` で D3 を全面書き換え（user 主導）

「**ASR は明示フラグ時のみ起動 / 通常運用は無料・即時 / 字幕なし遭遇したら明示的にコストを払う / 多言語は別問題: Topics の system prompt に『出力は常に日本語』を追加**」を user が指示。深さの違う 4 象限（字幕あり×日本語/英語、字幕なし×日本語/英語）に分解し、コスト試算（1 時間動画基準・月 60 本想定）:

| 手段 | 1h あたり | 月 60 本 | 多言語 | 字幕なし | 精度 |
|---|---|---|---|---|---|
| timedtext | $0 | $0 | △ 字幕依存 | ❌ | △ 自動字幕雑 |
| Whisper API | $0.36 | $21.6 | ✅ | ✅ | ◎ |
| Whisper local | $0 | $0 | ✅ | ✅ | ◎ |

→ **timedtext 主、`--asr` フラグで Whisper API opt-in、`--asr=local` は予約のみ非実装** に決定。design.md D3 全面書換え + D11「ASR 実装の構造」新設 + D9 に `OPENAI_API_KEY` 追加 + D10 にテスト 2 件追加 + Goals/Non-Goals/Risks/Migration 全更新。spec.md にも「ASR opt-in via --asr flag」「ASR transcript file format」の 2 Requirement 新設（6 シナリオ）

学び: **「コスト判断を人手に残す」設計パターン**。デフォルト無料、`--asr` でユーザが明示的にコスト発生を承認する形にすると、月額予算の超過リスクが運用上の人手判断で抑え込める。「自動でフォールバック」より「明示フォールバック」のほうが大量呼び出しでは安全

### Phase C — `/opsx:apply` で 55/62 タスク実装（§1-§9 + §10.1/§10.2）

1 セッションで以下を起こした（[pipeline/research/](pipeline/research/) 配下に新規 8 ファイル + tests）:

- **§1**: `youtube-transcript` / `@anthropic-ai/sdk` / `openai` を `package.json` 追加、`.env.example` に 3 キー追記、`npm run research` script 追加
- **§2**: `k_numbering.js` + `dedup.js` + tests（12 件）
- **§3**: `youtube_search.js` + tests（5 件、YouTube Data API v3 mock）
- **§4**: `transcript_fetch.js`（言語優先 `ja > en > その他`） + tests（7 件）
- **§4.5**: `audio_download.js`（yt-dlp ラッパ + tmp sweep） + `asr.js`（Whisper API + verbose_json + finally cleanup） + tests（10 件）
- **§5**: `topic_synthesize.js`（Claude Sonnet 4.6 + K005 few-shot + cache_control + 「日本語出力」prompt 指示） + tests（6 件）
- **§6**: `write_knowledge.js`（frontmatter + `[mm:ss]` 形式 + 衝突検出 + ファイル名サニタイズ） + tests（5 件）
- **§7**: `index.js`（CLI dispatcher、6 フラグ、auto-pick/force/dry-run）

§10 はユーザの実 API キー必要なので一旦停止。§10.1/§10.2 は dry-run なので走らせて exit 0 確認

### Phase D — user が API キーを `.env` 追記、§10.3 実生成 K006

`/opsx:apply` 再開、PIVOT「日本人 9 割が持つ『痩せ菌』の正体」（35 分動画）でキーワード検索:
- transcript 取得 OK（51 KB、duration 2109s、ja 字幕、`generator: youtube-transcript`）
- Topics 生成 OK（[K006_日本人9割が持つ「痩せ菌」の正体.md](dept/research/knowledge/Topics/K006_日本人9割が持つ「痩せ菌」の正体.md)、5 セクション + 8 投稿ネタキーワード、コスト ~$0.08）
- 校閲チェック: 観点が深い（短鎖脂肪酸の 3 走者リレー / 冷めたご飯 / ビタミン B1 が酪酸産生の鍵）、固有名詞ルール守れている（`Bifidobacterium`, `Akkermansia muciniphila`, `Blautia`, `Ornithine`, `Thiamine` の原語＋括弧訳）、数字保持（10-15%, 50%, 1%, 9割, 1/3）
- ⚠ Whisper 不要なのに発見: YouTube 自動字幕に誤字あり（「町内細菌」「探査死亡さ」「塔」）、Claude が文脈で正しく解釈していて運用 OK

### Phase E — PR #2 作成 → ユーザ merge → K006 校閲 commit

- PR #2 https://github.com/tatoflam/ThreadsPosts/pull/2 (29 ファイル、+2906/-7、`71f910d feat(research): K006+ Knowledge 自動生成パイプライン`)
- ユーザ merge 実施
- K006 校閲 → `status: draft_auto → ready` 別 commit (`0bb51f5 content(research): K006 校閲完了 → status: ready`)

### Phase F — §10.5 ASR smoke + 副産物 openai SDK v4 → v6 upgrade

- 字幕なし 60 秒動画（くるくるメディカル「大腸菌」）で `--asr` 起動
- ⚠ 1 回目 `npm install` でローカル Node v25 + openai v4 + ESM の `multipart upload` で `ECONNRESET` → SDK v4 → v6 upgrade で解消（commit `55f57e1 fix(research): openai SDK v4 → v6 で multipart upload の ECONNRESET を解消`）
- ⚠ 2 回目: OpenAI billing quota 不足で 429 → user 側で billing 設定 + auto-recharge 有効化（user 並走作業）
- 3 回目: ASR end-to-end 完全成功 ✅ → [K008_くるくるメディカル_大腸菌.md](dept/research/knowledge/Topics/K008_くるくるメディカル_大腸菌.md)
  - generator: `whisper-1` / lang: `japanese` (Whisper 自動検出) / 56s（60s 動画と一致）
  - タイムスタンプ `[00:00]` 形式保持
  - **Whisper 誤字 → Claude が文脈で正しく解釈**: 「イイコリ」→ `E. coli`、「肝菌」→ `グラム陰性桿菌`
  - 固有名詞保持: `E. coli`, `O157`, `HUS: Hemolytic Uremic Syndrome`
  - コスト: ~$0.006 ASR + ~$0.04 Topics = **~$0.05**

### Phase G — §10.6 多言語 smoke の罠

英語動画 TED-Ed「How food affects gut」(5 分) で実行:
- ⚠ `youtube-transcript` の優先順位 `ja > en` で **TED-Ed の JA コミュニティ翻訳字幕を取ってしまった** (lang: 'ja') → 「英語 transcript → Claude 翻訳」パスは **テスト未達**
- 「翻訳済 JA → Topics 整形」の別パスは成功 → [K007_食事が腸内細菌に与える影響.md](dept/research/knowledge/Topics/K007_食事が腸内細菌に与える影響.md)

校閲時に **3 件の bug 発見**（commit `8841d66 fix(research): K007 生成で発覚した 3 件の不具合を修正`）:

1. **markdown コードフェンス ` ``` ` 混入 + frontmatter 二重**: Claude が完全 markdown を ` ``` ` でラップして返却。私の system prompt が出力フォーマット例を ` ``` ` で囲んでいたのを Claude が literal コピーした
2. **`model: claude-opus-4-5` を hallucinate**: 我々の指定は `claude-sonnet-4-6` だったのに別 model 名を生成
3. **`(unknown)` / `(URL指定)` メタデータ**: `--url` モードで動画 metadata（title / channel）を fetch していなかった

修正方針: **Frontmatter を Claude に任せず、プログラム側で確実に上書き**。system prompt の出力例も plaintext に変更。+ docs に ffmpeg 必須を追記

### Phase H — 真の EN→JA Claude 翻訳パスを BBC で再検証

TED-Ed が JA 字幕迂回したので別動画で再試行:
- BBC Global の英語動画（1667 chars 英語 transcript）
- ✅ 完全日本語 Topics 出力: 4 観点 / 7 キーワード / 固有名詞（`Germ-free rodents`, `BBC Global`, `エキストラバージンオリーブオイル`）すべて適切
- ✅ 数字保持（2kg, 9 週間, 5 ステップ）
- → smoke script は削除して archive へ進む

### Phase I — `/opsx:archive research-pipeline-k006`

- archive 先: [openspec/changes/archive/2026-05-04-research-pipeline-k006/](openspec/changes/archive/2026-05-04-research-pipeline-k006/)
- specs sync:
  - 新規: [openspec/specs/research-pipeline/spec.md](openspec/specs/research-pipeline/spec.md)（15 Requirements、change-side の `## ADDED Requirements` wrapper 削除＋ Purpose 追加）
  - 更新: [openspec/specs/dept-organization/spec.md](openspec/specs/dept-organization/spec.md)（capability mapping 7→8）
- archive commit: `712ac50 chore: archive research-pipeline-k006 + research-pipeline spec を main へ反映`
- `active changes` 空、`openspec/specs/` 8 capability に整合

### 最終成果

| 項目 | 値 |
|---|---|
| タスク完走 | **62 / 62** |
| PR | #2 merge 済 |
| Knowledge 在庫 | K001-K005（legacy）+ **K006 / K007 / K008（新規生成、全 ready）** |
| K006 | PIVOT「痩せ菌」(35分)、`youtube-transcript ja`、$0.08 |
| K007 | TED-Ed「How food affects gut」(5分)、`youtube-transcript ja (auto-translated)`、$0.05 |
| K008 | くるくるメディカル「大腸菌」(60秒)、**`whisper-1`**、$0.05 |
| 合計コスト | **~$0.18** |
| OpenSpec capability | **7 → 8**（`research-pipeline` 新設） |
| commit 数（archive 含む） | 5（`71f910d` / `0bb51f5` / `55f57e1` / `8841d66` / `8e511a5` + archive `712ac50`） |
| tests | 197 → 203（bug fix 6 件追加） |

### 学び（横展開可能）

- **「コスト判断を人手に残す」opt-in 設計**: デフォルト無料、`--asr` で明示承認しないと有料 API は走らせない。月額予算の超過リスクは運用人手判断で吸収できる。「自動 fallback で気づかぬ間に課金爆発」より圧倒的に安全
- **LLM の system prompt は出力例を ` ``` ` で囲むと literal コピーされる**: Claude が ` ``` ` 込みで markdown を返してきた件は、system prompt の例示にコードフェンスを使ったのが直接原因。出力例は plaintext で、プログラム側でフォーマットを固定するのが安全
- **frontmatter を LLM に任せない**: model 名 hallucination や `(unknown)` メタデータの問題は、frontmatter を Claude が生成すると不可避。プログラム側で確実に上書きする pattern が安定運用には必須
- **多言語パスのテストは「コミュニティ翻訳字幕」で簡単に迂回される**: TED-Ed が JA 字幕を持っていたために `youtube-transcript` の言語優先順位 `ja > en` が真の EN→JA 翻訳パスをスキップした。多言語 smoke は「英語のみで JA 字幕がない動画」を選ぶ必要がある（BBC など報道系が確実）
- **「ローカル Node v25 + openai v4 SDK + ESM」の組合せで multipart upload が ECONNRESET**: openai SDK v4 → v6 upgrade で解消。Node v25 環境では SDK のメジャーバージョンを最新で揃える運用に
- **Whisper の音声誤字は Claude が文脈で吸収する**: ASR の生 transcript には固有名詞の誤字が含まれるが（「イイコリ」→ `E. coli`、「肝菌」→ `グラム陰性桿菌`）、Topics 生成段階で Claude が文脈から正しく解釈する。「ASR の精度が低くても下流の LLM が補正する」のは ASR + LLM 二層構成の堅牢性の根拠
- **OpenSpec の `## ADDED Requirements` wrapper は spec sync 時に削除する**: change-side の delta は wrapper 形式だが、main spec に sync するときは wrapper を外して plain spec に変換する。Purpose セクションは change-side からはコピーされないので別途追加が必要

### 次セッションでやること（user 明示の固定順序）

1. `/opsx:propose companify-pipeline-relocate` — `pipeline/*.js` を `dept/dev/pipeline/` へ
2. `/opsx:propose marketing-strategy-split` — 配信戦略を `dept/marketing/` 分離
3. **Drafts 在庫補充** — K006-K008 → `npm run generate -- --topic K00X --target "..."` で人実行（5/9 以降の在庫ゼロ問題を解消）
4. `/opsx:propose multi-tenant-bootstrap` — 2 アカウント目立ち上げ時

memory 保存済: [project_session_2026-05-04.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/project_session_2026-05-04.md)

see also: [[03_work/threadsposts]] / [[05_learn/youtube-knowledge-pipeline]] / [[05_learn/persona-driven-content-rules]] / [[06_output/2026-05]]

## /wiki-ingest（手動・ThreadsPosts cwd：ce4cb7d1 substantive + 51ec96dd/e69f095c meta-flip）

- session: 本セッション（手動 invoke、cwd=ThreadsPosts、5-4 朝に実行）
- 開始時 queue 3 行 unprocessed（unique 3）。dirty WIP 0（前回 5-3 23:36 JST の 22-cycle drain で全クリア済）
- **substantive 1**:
  - **ce4cb7d1**（ThreadsPosts、5-3 24:26 → 5-4 03:02 JST、約 2.5 時間 / 974 行 / 282 user msg）: research-pipeline-k006 全 62 タスク完走 — `/opsx:propose` 起票 → `--asr` opt-in 仕様変更 → `/opsx:apply` で §1-§9 実装 → §10 smoke (K006 生成成功 + Whisper auth エラー → openai SDK v4→v6 upgrade で解消 + K007/K008 生成 + 3 bug fix) → PR #2 merge → archive commit `712ac50`。Knowledge K006/K007/K008 全 ready、合計 ~$0.18、OpenSpec capability 7→8。詳細 narrative は本日 §1
- **meta-flip 2**:
  - **51ec96dd**（ThreadsPosts、5-3 23:20–23:35 JST）: 5-3 23:35 commit `f24ab4a` を出した /wiki-ingest run の本体。3 sessions process（4695d1ed substantive + e01596df substantive + d61bd11c meta-flip）、deferred 6 unique（22nd cycle）。narrative は [[02_diary/2026-05-03]] §10「22 サイクル目で deferred 6 をついに drain」前段に既出。本ランは frontmatter source 追記 + cursor 0 → EOF
  - **e69f095c**（ThreadsPosts、5-3 23:20–23:37 JST、51ec96dd と並走）: 5-3 23:36 commit `b96f859` を出した /wiki-ingest run の本体。22-cycle drain 成功 — sayama / saltmoon / woodone WIP を `git add -A` で取り込み、9 sessions process（substantive 2 + meta-flip 6 + missing 0）、deferred 0。narrative は [[02_diary/2026-05-03]] §10 後段に既出。本ランは frontmatter source 追記 + cursor 0 → EOF
- **dirty-target gate**: 全クリア。22-cycle drain 後の通常運用復帰
- 後処理: ingest-log.jsonl に 3 行追加、queue.jsonl で 3 行を `processed=true` に flip、cursors.json で 3 transcript path に EOF offset 設定、wiki meta 3 ファイル（index.md / log.md / _schema.md）リフレッシュ
- see also: [[05_learn/wiki-automation-pipeline]] / [[03_work/threadsposts]] / [[05_learn/youtube-knowledge-pipeline]]

## companify-bc-hybrid 完走 — B+C ハイブリッド第 1 弾を 1 セッションで起票→実装→archive→README→push（57a002bd、5-3 23:21 → 5-4 00:21 JST）

- session: 57a002bd  cwd: ThreadsPosts
- 起点: 5-3 evening の e01596df セッション末尾で「会社化（B+C ハイブリッド）方針」を宿題化、その後 ce4cb7d1 で `dept/research/` を先行着工（research-pipeline-k006 完走）。本セッションで満を持して **会社化提案そのもの** を `/opsx:propose companify-bc-hybrid` で起票
- 結果: **39/39 タスク完走、PR #1 マージ、archive commit `910eda0`、README.md 新規 + tasks tick-off `dbb7564` の 2 commit を `origin/main` push まで貫通**

### Phase A — 起票で「マルチアカウント観点」が抜けていることを user 指摘で修正

初版 proposal は「マルチジャンル」観点しか入っていない（`genres/<genre>/dept/...` の予約のみ、genre = account 暗黙同一視）状態。user から **「マルチアカウントで Threads 運用する観点は入ってる？会社化するなら必須」** と指摘 → `genres/<g>/{shared,accounts/<a>}/` の二層構造に組み直し:

- `shared/` = ジャンル横断資源（ナレッジ、商品 DB、戦略テンプレート）
- `accounts/<a>/` = アカウント固有資源（drafts、published、metrics、token、persona）
- 分離リソース: `Affiliates/products.yaml`（per-account 上書き可能）/ `Drafts` / `Published` / `Analytics/metrics.csv` / `posting_policy.yaml` / アクセストークン / persona 設定
- 共有リソース: `Knowledge/`（ジャンル横断ナレッジ）/ ピペットの実装コード（`dept/dev/pipeline/` は account 軸を超える例外）

→ proposal/design/spec/tasks 4 件を update、validate も pass

### Phase B — `/opsx:apply` 開始 → 即座に **artifact mismatch でポーズ**

tasks/specs/design は **Python `scripts/` レイアウト前提**で書かれていたが、リポジトリは **Node.js `pipeline/`** + 数個のトップレベル dir。section 1 開始直後にミスマッチ発覚 → 即停止して user 確認:

- 選択肢提示: (1) 現状の Node.js + 限定スコープに artifact 全更新して進める / (2) Python 化しなおす（範囲外） / (3) 中止
- user `1` 選択 → artifact 全更新（Node.js 化、scope を「dept/ skeleton + git mv 5 dir + path 参照更新」に縮小）→ 再 validate pass → 再開

学び: **artifact mismatch を smoke で発見したら即停止する原則**。タスクを進めながら型を変えると後戻りが大きい。stop / ask / re-plan の 3 ステップが正解

### Phase C — 6 section の実装（36/39）

| Section | 内容 |
|---|---|
| 1. dept/ skeleton | 6 dept (`research`/`content`/`marketing`/`affiliate`/`analytics`/`dev`) + `genres/` 各 README 作成 |
| 2. git mv | `Knowledge/` `Topics/` `Drafts/` `Published/` `Affiliates/` `Analytics/` を該当 dept 配下に移動 |
| 3. pipeline path 参照更新 | publish.js / resolve_rakuten_links.js / validate_frontmatter.js / schedule.js / lint_posts.js / backfill_frontmatter.js |
| 3.B | GH Actions workflows の path 参照更新 |
| 3.C | tests の path 参照更新 |
| 4 | CLAUDE.md / SKILL.md / docs/ の path 参照更新 |
| 5 | 152/152 tests pass + dry-run smoke OK（`git log --follow` のみ未 commit のため空、commit 後に効くと確認） |
| 6 | 7 capability すべて `openspec/specs/` で 1 回ずつ claim されているか cross-check |

### Phase D — 7 (release) は user 認可で実施

section 7（commit + PR + post-merge monitoring）は system 規則で user 明示認可必須。`1` 選択 → commit `c8637e7 refactor(repo): B+C ハイブリッド第1弾 — content/data ディレクトリを dept/ へ再編` → push → PR #1 作成 → user merge → archive commit `910eda0` → main 直 push がローカル hook で正しく拒否（PR review bypass 防止）→ user 手動 `git push origin main` + `git push origin main:archive/companify-bc-hybrid` で archive 完了

### Phase E — 「結局何ができるアプリ？」→ README.md 新規

user `「結局、現時点では何がどのようにできるアプリになったの？README.mdに簡潔に内容と運用方法をまとめてもらえると助かる」` → README.md 新規作成（107 行）:

- **何ができるか** — 8 機能 × 自動/手動の対応表
- **ディレクトリ構成** — 部署別 + pipeline / docs / openspec / genres の役割
- **運用フロー** — 週次の通常ループ (6 ステップ) + 3 workflow の自動実行表 + 例外対応
- **セットアップ** — `npm install` 〜 `npm test`
- **ドキュメント早見表** — `docs/`, `CLAUDE.md` への入り口

→ archive 内の `tasks.md` 7.1/7.2 の `[x]` tick-off と合わせて 1 commit `dbb7564 docs: README.md 新規追加 + archive tasks の 7.1/7.2 を完了マーク` → user 指示 `pushして。` で `910eda0..dbb7564` を `origin/main` に push 完了

### 学び（横展開可能）

- **artifact mismatch は smoke 段階で即停止**: 起票時の前提（Python 等）と実装時の現実（Node.js）がズレた時、タスクを進めながら型を変えると差分管理が破綻する。section 1 で気づいたら即 stop → user に「artifact 更新で進める / scope 変えない」の選択を投げて再 plan するのが正解
- **マルチアカウント観点は会社化提案の必須要素**: 1 ジャンル × N アカウントは会社運営の最初の必然（rate limit / ペルソナ並列 / シャドウバン耐性）。`genres/<g>/{shared,accounts/<a>}/` の二層構造を提案段階から組み込んでおくと、後で追加するより構造が綺麗
- **dev 部署は account 軸を超える例外**: pipeline コードを `genres/<g>/shared/dev/` に入れない方針は、コード fork の発生を防ぐため。tenant-path-resolver でも `dept='dev'` は reject する設計にする
- **README.md は会社化フェーズの自然な成果物**: 部署別 dir に再編した直後の「何ができるか/どう運用するか」を 1 ページにまとめると、user 自身が状態を再認識できる + 次セッション再開時の見取り図になる

see also: [[05_learn/local-first-anthropic-ops]] / [[03_work/threadsposts]] / [[06_output/2026-05]]

## /wiki-ingest 並走 2 件（34378f6f deferred / 930584ad processed; 5-4 03:02–03:12 JST）

- sessions: 34378f6f / 930584ad（同時刻に 2 個の手動 `/wiki-ingest` が並走、cwd=ThreadsPosts）
- **930584ad** は **本日 §1 + §2 を実際に書いたランナー**。ce4cb7d1 substantive narrative + 51ec96dd / e69f095c meta-flip を 1 commit で配信
- **34378f6f** は同時刻に走った 2 個目の手動 `/wiki-ingest` — race を察して **「auto-worker（= 930584ad）の出力を待つ」と判断、自分は yield**（commit/queue/cursor 触らず exit）
- 学び: **手動 `/wiki-ingest` の二重起動は yield 設計で安全に冗長化できる**。dirty-target gate と「auto-worker が走ってる時は待つ」の組合せで race 競合は回避可能

## 4 OpenSpec changes 起票 + 3 実装 Phase 1 + Local-First 確定（d40649e2、5-4 03:02 → 08:24 JST、約 5.5 時間）

- session: d40649e2  cwd: ThreadsPosts
- 起点: 前セッション（57a002bd）末尾の「次セッションでやること」固定順序リスト → user が冒頭で **「以下の順でよろしく！」** と明示指示で 4 件をキューイング
- 結果: **OpenSpec changes 4 件起票 + 3 件 Phase 1 実装 + 4 commit 完成（push 未実施）**

### #1 `/opsx:propose companify-pipeline-relocate`

`pipeline/*.js` を `dept/dev/pipeline/` へ移動する change。companify-bc-hybrid stage 1 で「pipeline は据え置き」例外を撤廃する後続。proposal / design / specs / tasks 4 artifacts validate pass

### #2 `/opsx:propose marketing-strategy-split` → 途中で **`marketing-cycle-bootstrap` 分離**

post-scheduler から配信戦略（`preferred_jst_hours` / `min_gap_hours` / `max_posts_per_day` / `daily_post_target`）を切り出して `dept/marketing/strategy/posting_policy.yaml` + 新 capability `posting-strategy`（marketing 所有）に移管する change。

途中で user から循環ループ視点の問い: **「戦略 → 入力 → 制作 → 収益化 → 実行 → 計測 → 戦略へ戻す、というクローズドループは事業として正しい姿。一方で今書いてる `marketing-strategy-split` の posting-strategy は『いつ出すか』だけに絞ってあって、本来 marketing が持つべき『次に何を research するか』『どの affiliate 枠を狙うか』が抜けてる」**

→ user 判断 **「1 を merge して 2 を `marketing-cycle-bootstrap` として別 change で起票の二段構え」**。posting-strategy の起票は merge して、循環ループは別 change として分離

### #3 Drafts 在庫補充 K006-K008 → D011/D012/D013

K006-K008 を topic に、target を以下で生成:
- D011 / K006 / target: ヨーグルトを習慣にしてるのに痩せない人 → 認知の置換（「悪玉菌減らせば痩せる」→「痩せ菌は短鎖脂肪酸リレーで生み出す」）
- D012 / K007 / target: 甘いヨーグルトに頼ってる人 → 認知の置換（「ヨーグルト = 腸活」→「食物繊維と多様性が本体」）
- D013 / K008 / target: 腸内細菌は善玉菌だけと思ってる人 → 意味の再定義（「大腸菌 = 悪」→「99% は無害、O157 等の少数派が病原性」）

3 本 lint/validate green。**衝突発見**: 既存 `published/D011_腸活スタジオ始動.md` に新 D011 が被る → `generate_post.js` が `published/` を numbering 計算に含めてない bug。今回は別 dir なので即時事故無いが、publish 時に file 上書きリスク → marketing-cycle-bootstrap の content-pipeline spec delta に **「D0XX numbering accounts for both drafts and published」** 要件として組み込み、Phase 1 tasks に含める

### #4 `/opsx:propose multi-tenant-bootstrap`

`dept/` 単一 tenant → `genres/gut-health/{shared,accounts/chokatsu-studio}/<dept>/` 二層化。新 capability `tenant-path-resolver` + `account-config`。Phase A=1-account 化 + Phase B=2 アカウント目立ち上げ。**design.md に "Anthropic API は GH Actions で自動 cron しない、Local-First" 原則を追記**

### `/opsx:explore` で **Local-First 原則確定**

ここで user が `/opsx:explore` に入って深掘り。最初の前提：「GH Actions に持っていけば運用が楽」が **間違い**と判明:

| 環境 | Anthropic API | コスト |
|---|---|---|
| ローカル (Claude Code) | Max plan x20 経由 | **既に支払い済 ($200/月固定)** |
| GH Actions | API key 経由 | **追加課金 ($/token、月 $5-30 推定)** |

→ Max plan x20 ($200/月) は OAuth ベースで GH Actions では使えない。GH Actions に Anthropic API を持ち込む = 別建ての従量課金 = **二重課金**。**Local-First** 確定:

- Anthropic API を使うステップ（research / generate / B フェーズ分析）はローカル Claude Code セッション（Max plan）で動かす
- GH Actions では **Threads / 楽天 API のみ呼ぶ workflow** を自動 cron（publish, sync_metrics, weekly_report, resolve_affiliates）
- workflow_dispatch の手動 Anthropic 起動オプションは将来の拡張として残す（`confirm_anthropic_cost: true` 必須）

### user 提案で **週次バッチ運用** 確定 → `/opsx:propose marketing-cycle-bootstrap`

user 続けて: **「その週の review, research, content, affiliate, schedule は一気に済ませていいんじゃないかな。それがうまくスケジュールされ、トリガーされて私の確認、指示出し依頼の通知がくるようにしてもらえれば」**

→ **週次バッチ** = OODA ループ + 集中タイム実行。1 セッション 30-90 分で「今週分終わり」の状態に到達。掘り下げて 6 フェーズ案を提示

user 追加: **「review と research の間に marketing（何に需要があり、どのような収益モデルを描けるかの戦略を立てる）のフェーズは必要ないかな？」**

→ 鋭い指摘。**Observe (REVIEW) → Act (RESEARCH 以降)** 直結で **Orient (戦略立案)** が抜けている = OODA 短絡。**B MARKETING フェーズ追加**:

- B-1 仮説出し（needs / supply のギャップから topic candidates）
- B-2 affiliate モデル（どの収益ループを今週試すか）
- B-3 persona 見直し（月初週は強制実行）
- B-4 戦略 ratify（status: draft → ratified、超えると無効化される `superseded`）

7 フェーズ確定: **A REVIEW → B MARKETING → C RESEARCH → D CONTENT → E AFFILIATE → F SCHEDULE → G PUSH**

`/opsx:propose marketing-cycle-bootstrap` 起票（Phase β = 2 段階）:
- Phase 1: 7 フェーズ + B-1〜B-4 + idempotency + `/weekly-cycle` skill（手動起動で完走）
- Phase 2: launchd plist + sessionStart hook + Stop hook の **3 段重ね reminder**
  - Layer 1: launchd 日曜 14:00 → macOS 通知 + iMessage to self
  - Layer 2: Claude Code sessionStart hook → check_weekly_due.js が exit 1 なら system-reminder inject
  - Layer 3: user が `/weekly-cycle` を直接叩く

新 capability 3: weekly-cycle-orchestrator (dev) / marketing-cycle (marketing) / weekly-cycle-trigger (dev)、modify 5: posting-strategy / research-pipeline / content-pipeline / performance-tracking / account-config

戦略アウトプット格納: `dept/marketing/strategy/weekly/<ISO_week>.md`（Section 1-7 構造）

### `/opsx:apply` 3 件連続実装

#### companify-pipeline-relocate（Phase 1 = 33/41 tasks）

47 R rename（`git mv pipeline/*.js dept/dev/pipeline/*.js`）+ package.json / GH Actions / docs / README / canonical specs の path 参照更新。203/203 tests pass。残 8 = post-merge / archive 作業

#### marketing-strategy-split（25/30 tasks）

- `dept/marketing/strategy/posting_policy.yaml` 新設
- `pipeline/lib/posting_policy.js` 新規（YAML loader + fail-fast validation、9 unit tests）
- `pipeline/schedule.js` を policy YAML 読込型に refactor（旧コンスタント排除、test 1 件調整）
- 212/212 tests pass（203 + 9 新規 + edge case 調整）
- policy YAML を 1 行壊して fail-fast 動作確認（schedule.js が落ちる→復旧 → タスク完了マーク）

#### marketing-cycle-bootstrap Phase 1（25/61 tasks）

- §2 共通インフラ: `pipeline/lib/iso_week.js` + 16 tests / `pipeline/lib/weekly_state.js` + 5 tests / 既存 `weekly_report.js` 改修 + 4 tests
- §4 `WeeklyCycle` skill 新設（後で kebab-case `weekly-cycle/` に rename）
- §5/§6: `dept/marketing/strategy/weekly/` directory + template + dept/marketing README 整備
- §7 動作確認: tz issue 発見（getISOWeek が UTC ベースなので Local 補正必要）→ Local week は 2026-W19 で system date と一致、tests 全 pass
- 237 → 訂正テストでの最終 pass を確認、validate clean

### skill 名 kebab-case 統一（user 指摘）

user `「/weekly-cycle は VS Code の Claude code プラグイン上では使えない？」` → 当初の登録名 PascalCase `WeeklyCycle` だったので user が叩くべきは `/WeeklyCycle`。`/weekly-cycle` 表記揺れが SKILL.md / tasks 内に多数。**rename**:

- skill ディレクトリ: `.claude/skills/WeeklyCycle/` → `.claude/skills/weekly-cycle/`
- frontmatter `name`: `WeeklyCycle` → `weekly-cycle`
- 関連ドキュメント言及も `sed` で統一

VS Code プラグイン / CLI / Web 全環境で `/weekly-cycle` で動作確認

### 4 commits 作成（push 未実施で stop）

user セッション再起動依頼 → user 推奨 + α 任せで 4 commit 分割:

```
d848bd8  content: Drafts D011-D013 補充 + multi-tenant-bootstrap 提案起票
ebccdaa  feat: weekly-cycle orchestrator (marketing-cycle-bootstrap Phase 1)
56ff446  refactor: 配信戦略を dept/marketing/posting-strategy へ分離 (marketing-strategy-split)
3397f6e  refactor: pipeline/ を dept/dev/pipeline/ へ移動 (companify-pipeline-relocate)
```

pre-commit hook が古い `pipeline/` パス参照していた companify の追従漏れを commit 1 に含めて解消

### 学び（横展開可能）

- **Max plan x20 を払っているなら Anthropic API は Local-First**: GH Actions に持ち込むと別建ての従量課金で **二重課金**。Local Claude Code セッション = Max plan 内で消費、GH Actions = 非 Anthropic workflow（Threads / 楽天 / 計測 API）のみ自動 cron する分離が最適。Max plan 解約時は GH Actions 移管も視野に入るが、現状は Local-First 一択
- **OODA で見ると週次サイクルは A=Observe / B=Orient / C-G=Act の構造**: REVIEW → MARKETING → RESEARCH → CONTENT → AFFILIATE → SCHEDULE → PUSH の 7 フェーズは「観察 → 戦略 → 実行」のクローズドループ。当初 6 フェーズ案で B (Orient) を抜きたくなるが、それは OODA 短絡で「先週の数字を見ても次の打ち手が決まらない」状態を作る。月初週は B-3 (persona 見直し) を強制実行する設計で、戦略の漂流を防ぐ
- **段取りオーバーヘッドが分散すると続かない → 集中タイムの週次バッチが続けやすい**: 1 セッション 30-90 分で「今週分終わり」の状態に到達するバッチ式が user の運用継続性にフィット。毎日 1 投稿でも準備に毎日触ると続かない、週 1 で 14 投稿分まとめて準備する形が認知負荷を下げる
- **3 段重ね reminder で運用継続性を担保**: 単一 trigger だとオフライン時の通知見逃し / Mac 開かない期間で取りこぼす。launchd（OS 通知 + iMessage）+ sessionStart hook（system-reminder inject）+ user 直接実行（`/weekly-cycle`）の 3 層冗長化で「今週やってない」状態を残さない
- **skill 名は kebab-case で統一が PascalCase より良い**: VS Code プラグイン / CLI / Web で全部共通の registry。user の指打ちは `/weekly-cycle` のような lowercase が自然なので、ディレクトリ名 + frontmatter name を kebab で統一しておく
- **`name` ≠ `search_query` パターンの再応用**: marketing-strategy-split で `posting_policy.yaml` を分離したのは、設定値の人間表記とプログラム読み取りを分ける思想の延長。`schedule.js` 内のコンスタント直書きから policy YAML 読込型に変えると、戦略変更が config 編集だけで完結する

memory 更新: [project_session_2026-05-04.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/project_session_2026-05-04.md) を本セッション内容で全面置換（4 changes 起票完了 + Drafts 在庫補充 + Local-First 原則）

see also: [[03_work/threadsposts]] / [[05_learn/local-first-anthropic-ops]] / [[06_output/2026-05]]

## 2026-W19 weekly-cycle 初実行 — `/weekly-cycle` を 1 セッションで完走、5 drafts scheduled、jitter 実装、push 自動化（a73c0aa2、5-4 08:24 → 09:50 JST、約 1.5 時間）

- session: a73c0aa2  cwd: ThreadsPosts
- 起点: 前セッション (d40649e2) で `marketing-cycle-bootstrap` Phase 1 を実装した直後 → user 「`/weekly-cycle` 実運用開始」
- 結果: **W19 (2026-05-04 〜 05-10) の戦略 ratify + drafts D011-D015 を 5/8〜5/10 に scheduled + 6 commits push 完了 + Step 0 fetch 漏れの learnings 抽出**

### Phase A REVIEW
- 先週レポート不在（初回サイクル）。`metrics.csv` 全 0 impressions（sync_metrics.yml がまだ走ってないか集計前）
- ドラフト在庫: D001/D003-D010 が 5/4-5/8 にスケジュール済（8本）、D011-D013 は draft 未割当、K001-K008
- weekly markdown `dept/marketing/strategy/weekly/2026-W19.md` を新規作成、Section 1 のみ埋めて user 確認
- ⚠ Step 0 で `git fetch` を回さなかったため、リモートに既に `weekly_2026-W17.md` / `W18.md` が存在することを A REVIEW で見落とした → 後段で発覚

### Phase B MARKETING（OODA の Orient フェーズ）
- B-1 需要シグナル: products.yaml と既存トピックの照合表で K002/K003/K005 が商品連動度高、K001 (乳酸菌) は連動 0 → トラフィック獲得用と割り切る
- user 提案: 外部 input として [mykinso のページ](https://mykinso.com/mykinsomedia/463) を参照 → 「プレ × プロ × シンバイオティクス」の整理は K003/K005 と整合
- B-2 収益モデル: 楽天 12 商品 hot（5-3 一括 r10.to resolve 済）/ Amazon は chokatsustudi-22 登録から 1 日（180 日 3 件ハードル待機）。新商品「ラクトフェリン+EC-12+ケフィア」は K001 とスタンス逆方向 → 次週候補メモのみ
- B-3 persona 見直し: W19 は月初週ではない（W18 が 5/1 を含む）、user 明示要求なし → skip
- B-4 戦略 ratify: 4 テーマ優先順位（1: K006 痩せ菌 / 2: プレ×プロ×シン整理 / 3: K007 食事と腸内細菌 / 4: 個人差）、楽天 5 商品重点（inulin / fructooligo / fiber_supplement / gut_microbiome_kit / equol_test）、posting_policy 変更なし → `status: draft → ratified`

### Phase C RESEARCH
- 必要 K0XX 数 0（K001-K008 で在庫十分）→ 新規生成なしでフェーズ完了

### Phase D CONTENT（脚注 URL 省略 A/B 開始）
- D011-D013 既存 lint pass、内容そのまま維持
- K006 から **D014 (海外サプリ流行に流されてる人 — アッカーマンシア vs ブラウティア)** + **D015 (炭水化物は太ると思ってる人 — 冷めたご飯のレジスタントスターチ化)** 新規生成
- ⚠ user 提案: **「D011 以降、出典のタイトルはつけるけど YouTube リンクはつけないパターンで publish して試してみない？毎回 YouTube 動画が Threads についてくるのはユーザーからウザがられる可能性ないかな」**
- → **D011-D015 全 5 本の脚注から YouTube URL を削除**、タイトル + publisher のみ残す。lint は URL を要求していないので安全。memory に [feedback_footnote_no_url.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/feedback_footnote_no_url.md) として記録（A/B 比較で D001-D010 の URL あり群と engagement 比較）

### Phase E AFFILIATE（明示マッピング + products.yaml 拡張）
- products.yaml の topics に K006/K007 追加（auto match を効かせるため）
- 明示マッピング: D011 → inulin_powder（第 1 走者の餌）/ D012 → fructooligo_syrup（無糖ヨーグルト + オリゴ糖）/ D014 → fiber_supplement_mix（既存菌の餌）
- D013（大腸菌二面性）/ D015（冷めたご飯）はアフィリ無し（商品連動弱い、トラフィック獲得用）
- 全 5 本 inject 後 lint pass（412/420/410 chars、500 以下）

### Phase F SCHEDULE（決定論的 humanJitter 実装）
- dry-run 一旦 5/8 (金) 19:00 → 5/10 (日) 19:00 で割当成立
- user 指摘: **「時間帯 19:00 でいいんだっけ。人間らしく数分ずらすのをよろしく」**
- → `dept/dev/pipeline/schedule.js` に `humanJitter()` 追加（**draft path の FNV ハッシュ → +60〜599 sec オフセット、決定論的**）。最初は +0〜9分59秒 範囲だったが「24 秒は人間らしさが薄い」で +1〜9分59秒 に調整
- 全 237 tests pass、5 件 scheduled、`status: draft → scheduled` 遷移
- 最終時刻: D011 5/8 19:04:24 / D012 5/9 08:08:37 / D013 5/9 19:04:53 / D014 5/10 08:06:12 / D015 5/10 19:09:18 JST

### Phase G PUSH（直 push 拒否 → settings 編集 → rebase → push 成功）
- 2 commit に分割: `feat(schedule)` (jitter) + `chore(W19)` (戦略 ratify + drafts + products.yaml)
- ⚠ `git push origin main` がローカル設定の **default branch 保護**で deny。さらに `.claude/settings.local.json` の編集も harness が「user の `C` 一文字では self-permission 編集として不十分」と拒否
- user 明示承認: **「`.claude/settings.local.json` に以下を追加することを承認した。`Bash(git push origin main)`、`Bash(git push origin main:*)`」** → 設定追加 → push 再試行
- ⚠ リモートに先行 2 commits 発見（auto-publish + metrics sync — **GH Actions が Local-First 構成通りに自動 cron で動いている証拠**）→ rebase で取り込み
- `cb8a62e..5727199 main → main` (6 commits) push 完了。GH Actions の publish.yml 次回 cron で D001/D003-D015 の順次自動投稿開始

### 学び（横展開可能）
- **A REVIEW の Step 0 に `git fetch` 必須**: リモートにある weekly_2026-W17.md / W18.md を pull していなかったため、本来 A REVIEW で読むべき先週レポートを見落とした → memory に [feedback_weekly_cycle_step0_fetch.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/feedback_weekly_cycle_step0_fetch.md) 記録、次サイクル前に weekly-cycle SKILL.md spec 改修 (openspec 起票) 検討
- **default branch 保護を緩める設定変更は user の明示・具体的承認が必須**: `C` の一文字回答では harness が permission 編集を拒否する。「`Bash(git push origin main)` を追加することを承認した」のような **対象ルールを literal に列挙** した承認が必要。今後の自動化は settings.local.json で許可ルールを明示しておく
- **GH Actions の自動 cron は Local-First の正しさを実体で証明する**: 本セッション中に GH Actions が `D005 auto-publish` + `metrics.csv sync` を独立に push してきた。ローカル Anthropic 使用セッションと並走しても衝突なし、Threads / 楽天 API のみ叩く workflow は GH 側に置けば自動運用が回る。**Anthropic を使う作業（Drafts 生成・戦略立案）はローカル、外部 API のみの作業（publish / metrics）は GH** の分離が正しいことを W19 サイクルで証明
- **決定論的 humanJitter は draft path ハッシュベースが正解**: 乱数だと「同じ draft を 2 回スケジュールすると別時刻になる」問題が出る。FNV ハッシュ → 60〜599 sec オフセットなら、同じ draft path に対して常に同じ jitter で再現可能。+0〜9分59秒の下限 0 だと「24 秒オフセット」で人間らしさが薄いので、下限 60 sec に調整
- **脚注 URL 省略 A/B のための D011 境界**: D001-D010 は YouTube URL あり、D011 以降は URL なしで、引用タイトル + publisher のみ。Threads のフィードに毎回 YouTube プレビューが付くのは（特に同じチャンネル連発時）ユーザに「広告化」と見えるリスク → engagement 差が出るかどうかを今週の計測対象に追加

memory 更新: [feedback_footnote_no_url.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/feedback_footnote_no_url.md) / [feedback_weekly_cycle_step0_fetch.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/feedback_weekly_cycle_step0_fetch.md) 新規

see also: [[03_work/threadsposts]] / [[05_learn/local-first-anthropic-ops]] / [[06_output/2026-05]]

## OpenSpec 3 changes archived — pipeline-relocate / strategy-split / cycle-bootstrap (Phase 2 不採用判定) (8b57f7c8、5-4 09:50-11:10 JST)

- session: 8b57f7c8  cwd: ThreadsPosts
- 起点: 前セッション (a73c0aa2、W19 weekly-cycle 初実運用) で 4 in-progress changes が滞留 — `/opsx:explore` で依存グラフを整理し残タスクの性質を仕分け
- 実態認識: companify-pipeline-relocate (33/41) と marketing-strategy-split (25/30) の **コード本体は既に main に landed 済**。残は PR/merge/monitor/archive の運用 housekeeping のみ。OpenSpec 上は in-progress でも実装は動いている → 「順序依存」は実質「archive 順序依存」

### 3 changes archive (`24cef9f` で commit、`5727199..24cef9f main → main` push)

- `archive/2026-05-04-companify-pipeline-relocate` — `pipeline/` → `dept/dev/pipeline/` 配下移動を main spec の `dept-organization` capability に反映 (`Asset-to-department mapping (first-stage)` Requirement の header 不一致を delta 側で訂正)
- `archive/2026-05-04-marketing-strategy-split` — `posting-strategy` capability 新規作成 (4 spec 同時更新: `dept-organization` ~1 / `post-scheduler` +1~3 / 新規 `posting-strategy` create)
- `archive/2026-05-04-marketing-cycle-bootstrap` — Phase 2 削除版 (詳細下記)

### Phase 2 不採用判定 — 「`/weekly-cycle` 手動運用で十分」

- Phase 2 = `weekly-cycle-trigger` capability (launchd / Stop hook / 通知の trigger 機構)、tasks 10.x-15.x
- 判断根拠: W19 サイクル実運用 (commit `5727199`) で `/weekly-cycle` 手動起動が 1 セッション 1.5h で REVIEW → PUSH 完走できた → わざわざ launchd まで組む価値は薄い
- 処置: `weekly-cycle-trigger/spec.md` 削除、tasks 10.x-15.x 削除、proposal.md に `Note (2026-05-04)` で不採用の判断根拠を記録、必要になれば別 change で再起票可能
- task 16.1 の遅延依存を解消: 「`multi-tenant-bootstrap` merge 後に `genres/README.md` 編集」→ 「現状の `genres/README.md` (予約状態) に追記、multi-tenant 着手時に再編集」と書き換えて cycle archive を multi-tenant 待たずに開放

### 副産物 doc 更新

- `README.md` に「週次サイクル (推奨)」節 — `/weekly-cycle` skill が通常ループの推奨運用であることを明示
- `docs/operations.md` に `/weekly-cycle` セクション — A REVIEW から G PUSH のフェーズ概要 + 運用ガード (Step 0 fetch、戦略 ratify gate、`--live` 確認 prompt) を記載

### 学び (横展開可能)

- **Phase 2 のような自動化機構は手動運用が回ってから判断する**: 当初設計では launchd + hooks + 通知の 3 段重ね reminder を提案していたが、実際に手動 `/weekly-cycle` を 1 回回したら自動化欲求が薄れた。**「設計時に欲しい機能」と「運用時に必要な機能」のズレ** を W19 cycle が露呈させた。同様パターン: 過剰設計の trigger 機構は MVP 後に評価
- **archive 時の MODIFIED Requirements は main spec header と完全一致が必須**: `/opsx:archive` が `Asset-to-department mapping` vs `Asset-to-department mapping (first-stage)` で fail。起票時の header と current main spec の header にズレがあると archive 直前で発覚する → delta 側を main の current 文言に合わせて再 archive
- **OpenSpec change の遅延依存は task 16.x のような「他 change merge 後の doc 更新」で発生しがち**: cycle-bootstrap task 16.1 が multi-tenant 待ちで archive を縛っていた。回避策は (A) doc 更新を current state 前提に書き換え (B) follow-up change に切り出し — 今回は (A) を採用

残: `multi-tenant-bootstrap` (0/71) のみ — trigger は「2 アカウント目立ち上げ」、当面寝かせ

237/237 tests pass。

see also: [[03_work/threadsposts]] / [[06_output/2026-05]]

## /wiki-ingest メタ — fdd9f971 (5-3 23:24-23:36 JST 4-session ingest) + 本ラン (5-4 朝)

- **fdd9f971** (ThreadsPosts、5-4 08:24-08:36 JST、12 分): 手動 `/wiki-ingest` 実行ラン。**4 sessions processed**（57a002bd companify-bc-hybrid + 他 3 件想定）+ 0 inbox + 0 deferred + 4 pages touched。前セッション直後の commit までが本ランで配信。本日 §3 の companify-bc-hybrid narrative はこのランの直接出力。frontmatter source 追記 + cursor 0 → EOF（614626 bytes）の meta-flip 待ちが本ラン
- **本ラン** (5-4 09:50 JST 以降): 手動 `/wiki-ingest`。queue で `processed: false` 残り 2 = fdd9f971 (meta-flip 1) + a73c0aa2 (substantive 1、本日 §6 として narrative 投入)。dirty-target gate 全クリア（vault 内のコンテンツ pages は uncommitted 修正なし、Obsidian workspace.json 等の noise のみ）
- 後処理: ingest-log.jsonl に 2 行追加、queue.jsonl で 2 行を `processed=true` flip、cursors.json で 2 transcript path に EOF offset 設定、wiki meta 3 ファイル（index.md / log.md / _schema.md）リフレッシュ

see also: [[05_learn/wiki-automation-pipeline]] / [[03_work/threadsposts]]

## playbook-process-revamp — 攻略本 + 外部 5 リンクから運用知見を取り込み 6 投稿型 / 3 セールスライティング型 / フックライブラリ / banned_patterns YAML / Rakuten event YAML を 1 セッションで起票→52/53 実装 (97d3f618、5-4 12:58 JST 終了)

- session: 97d3f618  cwd: ThreadsPosts
- 起点: user 提供の攻略本 (3818 行) + 外部 8 リンク (Notion 特典 3 件 + 楽天年間カレンダー + 競合リサーチ note + 分析の鬼 5 投稿サイクル + 公開 note 報酬上限記事 + Intage SNS 利用率 40 代女性 +5.6pt) を運用知見化する change を `/opsx:propose` で起票 → そのまま `/opsx:apply` で実装まで貫通
- 結果: **OpenSpec change 1 件起票 + 52/53 タスク完走 (5.4 のみ user 対話実行で deferred)、tests 237 → 247 (+10 新規)、main spec capability 11 → 12 想定 (archive 未実施)、未 commit (working tree に M 約 30 ファイル)**

### Phase 0 — Foundation files (15/15)

- `dept/content/templates/` 6 投稿型 (`product_single` / `summary_set` / `summary_tree` / `room_redirect` / `shop_redirect` / `shop_search_redirect`) — 各テンプレに「## 推奨セールスライティング型」セクション
- `dept/content/templates/patterns/` 3 セールスライティング型 (`prep` / `pasona` / `aidma`、`none` は frontmatter 値のみ)
- `dept/content/hooks/gut-health.md` — 40+ フック構文 (compliance: ok / borderline / banned 3 階層)
- `dept/content/style/banned_patterns.yaml` — 文体機械検証用 (CLAUDE.md「使ってはいけない表現」の YAML 化)
- `dept/marketing/strategy/rakuten_events.yaml` — 楽天年間カレンダー (毎月1日 / 5・10・15・20・25日 / 18日 / マラソン / スーパーセール / ブラックフライデー / 大感謝祭 / イーグルス感謝祭 / ブランドデー、2026 年末まで 10 events)
- `dept/research/competitors/gut-health.yaml` — skeleton (a.r10.to 検索 / フォロワー 3k-20k / 楽天リンク率 70%以上 を満たす爆益アカウント DB)
- `dept/marketing/strategy/posting_policy.yaml` に新キー追加: `type_slots` (型別の配信スロット) / `jitter_minutes_max` / `event_boosts` (楽天イベント窓ブースト)

### Phase 1 — Pipeline code (14/14)

- `dept/dev/pipeline/lint/rule_lint.js` — YAML banned-patterns + hook validation + frontmatter `template_type` / `copywriting_pattern` 必須チェック (テスト API は既存互換維持)
- `dept/dev/pipeline/generate_post.js` — `--template` / `--pattern` / `--genre` フラグ追加、6 型 + 3 パターン validation
- `dept/dev/pipeline/schedule.js` — `type_slots` 優先 / 決定論的 jitter (W19 で導入済) / `event_boosts` 窓検出
- `dept/dev/pipeline/account-research/index.js` — `competitors` / `products` / `promote` 3 サブコマンド + npm scripts (`research:competitors` / `research:products` / `research:promote`)

### Phase 2 — Performance tracking (7/7)

- `dept/dev/pipeline/sync_metrics.js` — `first_30min_engagement` (25-35 分窓で reach × engagement_rate を取得) / `buzz_class` 自動派生 (none / micro / standard / mega) / `audience_match_flag` ユーザ手動入力で上書き禁止 / 新 CSV カラム
- `dept/dev/pipeline/weekly_report.js` — Template-type ROI セクション / Buzz-class summary / 30-min early signal セクション

### Phase 3 — Skills + CLAUDE.md (7/7)

- `.claude/skills/weekly-cycle/SKILL.md` — B-5 楽天イベントチェック追加 / B-1 爆益アカウント review / D CONTENT 40:50:10 構成比ガード (商品紹介 / ナレッジ / 体験型 のバランス)
- `.claude/skills/PostCreation/SKILL.md` — テンプレ → パターン → フック の読み込み手順 (型選択は構造、文体は語り口の二層分離を明文化)
- `CLAUDE.md` — 「投稿生成フロー (型選択 → 構造 → フック)」セクション追加 + 6 投稿型の収益期待ランク (`shop_search_redirect` > `shop_redirect` > `summary_tree` > `summary_set` > `room_redirect` > `product_single`) + 文体ルールは型選択に関わらず常時遵守を明記

### Phase 4 — Backfill + validation (4/5、5.4 deferred)

- D001-D015 + published 16 件全てに `template_type: product_single` / `copywriting_pattern: none` 遡及付与 (working tree M 30 ファイルの主因)
- `npm run lint:posts` 16/16 pass、`schedule.js` no-op (整合)、`weekly_report.js` 新セクション正常表示確認
- 5.4 (6 型 smoke test) は PostCreation skill 経由で Anthropic API call が発生するため user の対話実行に委ねる

### Phase 5 — Memory + READMEs (5/5)

- 新 memory 2 件: [feedback_post_templates.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/feedback_post_templates.md) (6 型ランクは攻略本由来、8 週蓄積まで観察フェーズ) / [reference_rakuten_calendar.md](/Users/tato/.claude/projects/-Users-tato-repo-github-tatoflam-ThreadsPosts/memory/reference_rakuten_calendar.md) (公式 https://calendar.rakuten.co.jp/cal/8196 を rakuten_events.yaml に手動同期、月次更新)
- `dept/marketing/` / `dept/research/` / `dept/content/` の README を新ファイル群対応に更新

### 外部リンク取材結果 (operational insights)

- 楽天年間カレンダー (rakuten_events.yaml の根拠): 毎月 1 日 / 5・10・15・20・25 日 / 18 日 + マラソン / スーパーセール / ブラックフライデー / 大感謝祭 / イーグルス感謝祭 / ブランドデー
- 競合リサーチ裏技: a.r10.to 検索 / フォロワー 3k-20k / 楽天リンク率 70% 以上 / ChatGPT DEEP research
- 分析の鬼: 5 投稿サイクル (挨拶 → 商品 → 有益 → 日常 → バズ) → CONTENT 構成比 40:50:10 の元ネタ
- note 公開記事: 報酬上限 1,000 円 / 商品・3,000 円 / 月、a.r10.to、24h cart / 89 日購入
- Intage: 40 代女性 SNS 利用率 +5.6pt (audience_match_flag のターゲット根拠)
- Notion 特典 3 (フック集 / バズショップリスト / プロフ作成くん): 認証 gated → 取得不可 (タイトルのみ)

### 学び (横展開可能)

- **「攻略本 + 外部リンク → 運用知見 YAML / markdown 化」の OpenSpec 変換パターン**: 3818 行の生資料を 6 spec delta に分解し proposal/design/specs/tasks 一式で起票すると、そのまま `/opsx:apply` で実装まで一貫できる。生資料を CLAUDE.md に直接ベタ貼りするより spec 化のほうが横展開しやすい
- **型選択 (構造) と文体 (語り口) は別レイヤで管理する**: 6 投稿型は構造の選択肢、3 セールスライティング型は構造内の論理展開、文体ルールは語り口でどの型でも常時遵守 — の 3 層分離。CLAUDE.md でこの分離を明文化することで「型選択 → 文体生成」の判断順序が固定される
- **収益期待ランクは攻略本の主張をそのまま採用、検証は 8 週後**: `shop_search_redirect` > `shop_redirect` > `summary_tree` > `summary_set` > `room_redirect` > `product_single` の収益ランクは攻略本由来 → 全テンプレを `product_single` から脱却させる動機になる。8 週分の `template_type` × ROI データが weekly_report.js で蓄積されてから検証
- **banned_patterns YAML 化で文体ルールを CLAUDE.md とコード両方から参照可能に**: 「使ってはいけない表現」を CLAUDE.md (人間用) と banned_patterns.yaml (lint 用) の二層で同期しておくと、人間生成と lint 機械検証で同じルールが効く
- **PostCreation skill の 6 型 smoke test は API call ありで deferred 妥当**: `/opsx:apply` の中で 6 型 × Anthropic API call 6 回は意図せぬ課金リスク → user 対話実行に委ねるのが Local-First 原則と整合

memory 更新: feedback_post_templates.md / reference_rakuten_calendar.md (MEMORY.md インデックス追記済)

### 残タスク (次セッション)

- 5.4 (6 型 smoke test) を user 対話で実行 → 各型の lint / scheduling 動作確認
- `/opsx:archive playbook-process-revamp` で main spec に `account-research` / `post-templates` capability 反映 (capability 11 → 13 想定)
- D016 以降の生成で 6 型のいずれか (`shop_search_redirect` 既定) を採用、`product_single` から脱却

see also: [[03_work/threadsposts]] / [[05_learn/persona-driven-content-rules]]

## /wiki-ingest メタ — 7a941d5c (5-4 11:10 JST、8b57f7c8 substantive narrative 配信ラン) + 本ラン (5-4 13:00 以降)

- **7a941d5c** (ThreadsPosts、5-4 09:50-11:10 JST 完了): 手動 `/wiki-ingest` 実行ラン。queue で `processed: false` だった **8b57f7c8 substantive (OpenSpec 3 changes archive batch) + e723638b meta-flip** の 2 sessions を処理 → 本日 §「OpenSpec 3 changes archived」narrative + 「2026-05-04 update — OpenSpec 3 changes archive batch」を threadsposts.md に投入、06_output/2026-05.md に archive push エントリ追記、commit `45e85a5` で配信。本ランで本セッション自体の cursor 0 → EOF (557714 bytes) meta-flip
- **本ラン** (5-4 13:00 JST 以降): 手動 `/wiki-ingest`。queue で `processed: false` 残り 2 = 7a941d5c (meta-flip 1) + 97d3f618 (substantive 1、本日 §「playbook-process-revamp」として narrative 投入)。dirty-target gate 全クリア (`02_diary/2026-05-04.md` / `03_work/threadsposts.md` 共に vault 側 working tree clean、Obsidian `.obsidian/workspace.json` の noise のみ)
- 後処理: ingest-log.jsonl に 2 行追加、queue.jsonl で 2 行を `processed=true` flip、cursors.json で 2 transcript path に EOF offset 設定、wiki meta 3 ファイル (index.md / log.md / _schema.md) リフレッシュ

see also: [[05_learn/wiki-automation-pipeline]] / [[03_work/threadsposts]]

## /opsx:archive playbook-process-revamp 完了 — main spec capability 11 → 13 (cef7a3c1、5-4 20:32 JST)

- session: cef7a3c1  cwd: ThreadsPosts
- `/opsx:archive playbook-process-revamp` 起動。validate 結果: artifacts 全て done、tasks 52/53 完了 (5.4 = 6 型 smoke test、Anthropic API 課金回避で intentionally deferred、tasks.md ノート明記)
- AskUserQuestion で「sync to main specs (推奨)」「skip sync」「abort」を user に提示 → user は **sync** を選択
- Agent 経由で sync 実行: ADDED 2 capability (`account-research` / `post-templates` 新設、main spec ファイル新規作成) + MODIFIED 4 capability (`content-pipeline` / `posting-strategy` / `performance-tracking` / `marketing-cycle` のヘッダ完全一致 merge)
- archive collision check OK → `git mv openspec/changes/playbook-process-revamp openspec/changes/archive/2026-05-04-playbook-process-revamp/` 実行
- `openspec validate --specs --strict` で 15 main specs 全 PASS
- ⚠ archive folder は working tree に **untracked** 状態で残った (commit されず) — 次セッション以降で commit 必要
- 結果: capability 11 → **13** (新規 2)、in-progress changes は当初 `multi-tenant-bootstrap` のみとなったが、3h 後の BAN 対応で別 3 change が起票されることになる

see also: [[03_work/threadsposts]]

## Threads アカウント BAN — 3 OpenSpec changes で運用 pivot (c02fca64、5-4 23:02 JST、commit `2ec9509` local-only)

- session: c02fca64  cwd: ThreadsPosts
- 5-4 中に `@chokatsu_studio` (Threads + 連携 IG) が突如 BAN。Meta 通知文言:「弊社のテクノロジーにより…テクノロジーにより措置が講じられました」「再審査はリクエストできません」「あなたの情報はすべて完全に削除されます」 → **ML 自動分類器による enforcement、人間レビューなし、最重 tier、IP 変更だけでは戻らない違反種別**
- 仮説: 「IP 変更 (GH Actions → local) より Account が人間に見えるか が本丸」(inauthentic / automation 分類器発火)。BAN 要因の太い候補は **アフィリ密度 > 新規 × API 即時開始 > テンプレ感 > 機械的 cadence > GH Actions IP > UA**
- 決定事項 (user 確定):
  1. 別ジャンル / 別ハンドルで再起 (chokatsu_studio は廃止)
  2. 別ドメイン振り直し、IG も新規作成
  3. 電話番号は別を使う (もう 1 つ手元にある)
  4. 月 50 万 / 3 ヶ月目標に下方修正 (旧 月 10-1000 万から)
  5. アーキテクチャは Option C (publish のみ local + metrics/affiliate は GH Actions read-only) + B 寄り (擬人化重視)

### 起票・update した 3 change (`/opsx:propose` × 2 + 既存 update × 1)

- **新規 [account-pivot-warmup]**: 5-phase warmup (Week 1 観察 / Week 2 ソフト立上げ / Week 3 段階導入 / Week 4 通常移行 / Week 5+ 通常運用) を `posting_policy.yaml` の `phases` キーで表現。`publish.js` が phase 上限を超える post を hard reject するゲート。新 capability `account-warmup-protocol` (5-phase ramp) + `account-isolation-hygiene` (9 項目分離要件 + PII scrub)。delta `posting-strategy` / `post-scheduler` / `account-config`。tasks.md は **80 タスク** (Phase 0 ジャンル選定 → multi-tenant 最小前倒し → playbook 反映 → policy 拡張 → schedule 初期化 → publish.js gate → tests → docs → dry-run)
- **新規 [publish-relocate-local]**: 公開アーキ刷新。`publish.js` を local 起動 (新 skill `/post-due`) のみに限定、GH Actions `publish.yml` は削除、`sync_metrics.js` も local 化 (defense in depth、Threads API call は全部 residential IP に集約)。新 capability `local-publish-runner` (起動契約 / token 配置 / exit code / startup log) + `local-metrics-sync` (起動・A REVIEW 連携・GH secrets 削除義務)。delta `post-scheduler` (`--live` 削除 / GH Actions refuse / token 経路修正) / `performance-tracking` / `dept-organization` (`_archive/<slug>/` レイアウトと PII scrub の正式化) / `weekly-cycle-orchestrator` (A REVIEW 24h sync gate、G PUSH から publish 切離し)。tasks.md は **73 タスク**
- **既存 update [multi-tenant-bootstrap]**: BAN 前提崩壊で proposal/design/tasks 全面書き換え (option A)。Phase A (1-account 化) + Phase B (chokatsu-lab) → 単一 phase (新 tenant 単独立上げ) に縮約。token は GH secrets `THREADS_TOKEN_<UPPER>` → local `~/.config/threads-posts/.env` 経由。account-config `token_secret_name` → `token_env_var`。GH matrix 化 scope 外 (publish.yml は publish-relocate-local で削除)。具体 slug `gut-health` / `chokatsu-studio` / `chokatsu-lab` → `<G>` / `<A>` プレースホルダー (Phase 0 完了後置換)。`_archive/` 認知を `dept-organization` spec で正式化。tasks.md は 71 → **73 タスク**
- 3 change いずれも `openspec validate` 通過、commit `2ec9509 openspec: pivot 3 changes for Threads BAN response` (28 files、+2165 / −249) を local main に積んだ。**未 push** (`origin/main..HEAD = 2ec9509` のみ)

### 3 change の依存グラフ (確定版)

```
multi-tenant-bootstrap (新 tenant scaffolding)
  ├ depends on: account-pivot-warmup Phase 0 (slug 確定)
  ├ depends on: publish-relocate-local 旧 tenant archive 完了
  └ provides: tenant_paths.js / account_config / per-account paths

account-pivot-warmup (warmup + isolation)
  ├ depends on: multi-tenant-bootstrap (account-config schema 拡張先)
  └ provides: phase gate / warmup spec / hygiene checklist

publish-relocate-local (公開アーキ刷新)
  ├ independent (技術的依存なし)
  ├ provides: env loader / /post-due skill / GH secrets 削除 / 旧 tenant archive
  └ same publish.js を編集するが箇所が重ならない
```

推奨 merge 順: `multi-tenant-bootstrap` → `publish-relocate-local` → `account-pivot-warmup` (git mv 範囲が交差するので実装時に commit 順序再確認)。

### 学び (横展開可能)

- **Meta 自動分類器の文言で違反種別を読み解く**: 「再審査リクエスト不可」「情報すべて削除」「テクノロジーにより措置」は **inauthentic / automation 分類器の最重 tier** signature。commercial policy 違反なら通常 reason がもう少し具体。IP 変更だけで隠れる相手ではない判断材料
- **BAN 要因はスタックする — 単一原因に絞ると 2 度目を踏む**: アフィリ密度 + 新規 × API 即時開始 + テンプレ感 + 機械的 cadence + GH Actions IP + UA を全部疑い、warmup プロトコル (Week 1-5+) で段階的に humanness signal を積む方が安全。`posting_policy.yaml` の `phases` キーで phase ごとの「投稿/日 上限 / affiliate ratio / 必須エンゲージ」を hard reject ゲートに乗せる
- **既存 OpenSpec change は scrap せず option A (update + apply)**: BAN で前提崩壊した `multi-tenant-bootstrap` を scrap して新規起票 (option C) より、proposal/design/tasks を BAN 文脈に書き直す option A の方が編集量も OpenSpec 履歴ノイズも少ない。「BAN を機にスコープ刷新した」事実が archive 時に記録される
- **3 change 並走は merge 順序と「編集箇所の非交差」で成立させる**: 技術独立 (`publish-relocate-local`) と依存関係 (`multi-tenant-bootstrap` ⇄ `account-pivot-warmup`) を分離。同じファイル (publish.js) を複数 change が編集する場合は箇所が重ならない設計にする
- **revenue target は incident で動的に下方修正する**: 旧 月 10-1000 万 → 新 月 50 万 / 3 ヶ月。warmup で 1 ヶ月失う前提で目標再設定 (memory `project_revenue_target.md` 同期)。incident で目標を動かさない方が「今すぐ通常運用に戻したい」圧力で warmup を破る誘惑になる

### 残タスク (次セッション以降)

- pre-existing 未 commit work (skills / drafts / pipeline / product DB / `archive/2026-05-04-playbook-process-revamp/` フォルダ) の整理 commit (BAN pivot とは別軸で個別に分けるか archive 判断)
- `account-pivot-warmup` Phase 0 (新ジャンル選定リサーチ) 着手 → 3 change の slug 確定で全体 unblock
- `multi-tenant-bootstrap` Section 4-5 (resolver 実装 + pipeline refactor) は slug プレースホルダーのまま先行実装可能
- 3 change を draft 状態で PR 切る準備 (push to origin)

see also: [[03_work/threadsposts]]
