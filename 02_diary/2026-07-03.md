---
title: 2026-07-03
category: 02_diary
tags: [diary, project:meguru-pm-report, project:habi-bff, topic:openspec, topic:doc-diff-carryforward, topic:report-phase-progress-render, topic:wiki-ingest-meta, topic:weekly-report-2026-07-03, topic:aws-profile-account-mismatch, topic:habi-dev-deploy, topic:step-debug-ab-comparison]
sources: [e890279a-c5ba-4e99-ac91-42532a75dc57, cfb8d309-b1b4-4f6c-8129-dbfadec0513d, 0672b33c-e5cf-48e6-9b16-ffd2c240fdab, 7572d03f-86ee-4cee-bd0e-16b9832f2673, a1f18d16-2e33-43a8-ab22-1e5535946301, 2082d6eb-5bac-4ddc-aeb1-848c0fb1fb37, 0b8942be-9a62-4d22-84b9-43caf91959c4, 6d328a3b-7b3e-44e1-bfd7-29bc66f889a7]
updated: 2026-07-04
---

# 2026-07-03

## 10:30  run-117 ingest — MeguruPMReport 6-30 フルセット + Doc 差分 carry-forward 移行 + archive 2 件 + 3 wiki-ingest meta-ack

- session: 自身 (`/wiki-ingest` run-117)  cwd: meguruit/MeguruPMReport
- run-116 (2026-07-02 17:50 JST) 以降の enqueue = **4 セッション**（v cwd ×3 + MeguruPMReport cwd ×1）。substantive は `e890279a`（MeguruPMReport 週次 6-30 + memo→Doc差分 carry-forward への置換 + archive 2 件）1 件、残り 3 件は自身 run-116 とその race-guard の meta-ack。
- **e890279a (MeguruPMReport, 2026-07-02 22:38 → 07-03)** — 22:38 起動、日次パイプラインが朝 9:00 に grabbers を回した直後のスナップショットを使って `/weekly-report 2026-06-30` フルセット生成 → publish → 途中で工程チャートのデグレ発見 → renderer 修正 → 再 publish → 溜まっていた commit 整理 → `replace-memo-with-doc-diff-carryforward` change を起票・実装・archive → 未 archive 5 件のうち実装完了 2 件（`add-jooto-checklist-item-backfill` / `exclude-ordinance-tasks-from-overdue`）を archive、という長い一気通貫セッション。詳細は [[03_work/meguru-pm-report]] の本日 H2 に反映、5 commits × 3 push の外部公開は [[06_output/2026-07]] に列挙。
- **cfb8d309 (v, 2026-07-02 17:54)** — `/wiki-ingest` 起動時に既に他 `/wiki-ingest` プロセス（run-116 self-identifying）が並行実行中と検知 → **race 保護で自己停止**。substantive 処理は run-116 側が完遂（`4e96953`）。この session 自体は diary へ meta-ack のみ。
- **0672b33c (v, 2026-07-02 17:54)** — 同じく `/wiki-ingest` を起動したところ 3 background auto-ingest worker（PID 56861/56884/57344）が並行実行中と検知（同一 `ba64389c` / `92563435` を対象にしていた）→ **runaway parallel workers 検知で自己停止**。diary へ meta-ack のみ。
- **7572d03f (v, 2026-07-02 17:56)** — `/wiki-ingest` run-116 として実際に完走した session。sessions: 6 processed / 9 deferred / 4 pages touched / 0 unsortable、commit `4e96953`（未 push）。substantive land は既に vault 側に反映済み。diary へ meta-ack のみ（source id はここに追加）。
- 3 vault session（cfb8d309 / 0672b33c / 7572d03f）は日中 17:54–17:56 の 2 分間に人が 3 回 `/wiki-ingest` を叩いた結果。うち 1 つが実処理、2 つが自己停止で race を回避した挙動そのもの。**「並行起動しても被害が出ない」という保護の実地検証** としては良い記録。

### e890279a の内訳（substantive）

このセッションで一度に片付けた事項が多いので `03_work/meguru-pm-report.md` 側で詳述。ここは箱書きのみ:

1. **weekly-report 2026-06-30 フルセット + publish 2 回**（初回 → 工程チャート renderer デグレ発見 → 修正 → 再 publish、同一 Doc へ冪等 update）
2. **`replace-memo-with-doc-diff-carryforward` change 完走**（propose→apply→archive、`- メモ:` 固定欄を廃止、publish 直後に自 Doc をエクスポートして `state/doc_snapshot/{run_date}.md` に保存、翌週これと人編集後 Doc を差分して整形差を相殺、真の追加行を保持ブロック化）
3. **未 archive 5 changes のトリアージ + 実装済み 2 件を archive**（A グループ = このリポジトリ内で実装完了 → archive、B グループ 3 件 = 別プロジェクト Bizuayeu 標的の change / #4 は #3 blocked / 大規模移行未着手、archive せず据え置き）
4. **`config/projects.csv` ロスター刷新 27 案件**（削除 5 = 済証交付後の後期案件、追加 8 = 新規 4 + 継続 4、担当変更 3）— publish 前に user 確認、以後の週次はこの新ロスターで走る
5. **統合表 H 列「デザイン監修」追加の齟齬チェック → 齟齬なし**（`integration.json` はヘッダ名でキー化、負荷ビューは `意匠担当` 参照で `デザイン監修` を読まない、Milestone 別シート、実値は東十条3丁目のみで負荷対象外）
6. **commit / push 未整理分の解消**（5 commits pushed to `origin/main`: `0ddfe8b..d2b3986` の 3 件 + `9e51203` + `436c577`）

### 工程チャート renderer デグレ（今後の再発防止メモ）

`plugins/drive-publisher/scripts/render_html.py` の `parse_phase_progress` は各工程行が `- {N}. {工程名}: {内容}` の**ダッシュ箇条書き**であることを `_PHASE_HEADER_RE = ^-\s+\d+\.…` で要求する。ダッシュを外した番号リストにするとパターン不一致で HTML 側は「工程ブロックチャート」ではなく **生 Markdown フォールバック**に落ちる（15 phase-row → 0 phase-row）。今回はセッション内で発見 → 修正 → 再 publish で復旧、[[memory:project_drive_publisher_renderer_coupling]] の「HTML レンダラは見出しレベル・箇条書き形式に依存」に該当。**markdown を弄って renderer と食い違わせない、または renderer を先に見て markdown 側の書式を合わせる** のを維持。

## 10:30  Deferred this run — chronic defer 49 日目

前回と同じ vault 側 dirty 3 ファイルが未解消:

- `03_work/todobot.md`
- `04_life/minami-gakudo-fubokai-2026.md`
- `06_output/2026-05.md`

上記に加えて過去日 `02_diary/2026-05-23.md` も dirty のまま。この 4 ファイルが commit or stash されるまで下記 10 セッションは drain 不能:

- **ToDoBot ×8 セッション** (`e594cbdd` ×2, `87c832cd`, `6c36b23b`, `74ef607c`, `b2998a84`, `41a0fb6a`, `e4dd720d`) → `03_work/todobot.md` dirty ブロック
- **gakudo-assingment ×2 セッション** (`e19b11f3` = 執行委員 assignment 世帯優先度ロジック追加 + venv 整備, `b7a41f9f` = SNAPSHOT_DATE bump) → `04_life/minami-gakudo-fubokai-2026.md` + `06_output/2026-05.md` dirty ブロック

（本日は user が MeguruPMReport 側の作業に集中していたので vault 側の dirty はそのまま。「まとめて刻む」タイミング待ち）

see also: [[03_work/meguru-pm-report]] / [[06_output/2026-07]]

## 10:36 → 22:18  run-117 直後の新規 4 セッション — 週次 07-03 publish + habi-bff dev deploy + AWS プロファイル取り違い解消 (run-118 で拾い上げ)

- session: 自身 (`/wiki-ingest` run-118、2026-07-04 起動)  cwd: tatoflam/minami-gakudo-tshirt
- run-117 (2026-07-03 01:36Z commit `ebf2615`) 以降に 4 セッション enqueue。substantive 2 件 (`2082d6eb` MPM 週次 07-03 + `6d328a3b` habi-bff dev deploy) + meta-ack 2 件 (`a1f18d16` = run-117 自身の SessionEnd 遅延 enqueue / `0b8942be` = 09:30 の nothing-to-ingest 空振り)。加えて `6a65883e` (minami-gakudo-tshirt) の transcript が **missing** で `hook-errors.log` 送り。
- **2082d6eb (MeguruPMReport, 2026-07-03 01:29 → 02:00 JST)** — 前セッション `e890279a` の 07-03 未明終了直後、user 発意 「最新のレポートを作成して。各ソースから対象の全案件について取りこぼしがないように。遅延、未解決の状況は詳しく記載すること。」で `/weekly-report` 全 27 案件フルセット (前倒し publish なし = 1〜9 順序厳守: baseline sync → Gmail 全 27 案件 → 6 項目要約・工程グリッド・条例マトリクス・負荷ビュー → **Milestone writeback `--scope all` (8 セル FALSE→TRUE、巻き戻し 0、integrity 保持)** → **Milestone 再取得** → **工程グリッド再生成 (3 案件前進を反映)** → publish)。途中 Firebase 認証切れ → **user が別ターミナルで `firebase login --reauth` を実施** → Drive doc は既に in-place 更新済 (doc id `1R9Vxm7XeFg…3EMU`、重複なし = idempotent 確認済) を確認、Firebase デプロイのみ再実行して meta 追記・`latest_state.json` を更新して完走。1 点手動修正: 高田馬場「直近予定: 仮受（意匠）」を 7 日窓外 (8/7) につき Milestone サブ行の条件不成立で削除。詳細は [[03_work/meguru-pm-report]] 07-03 H2、外部公開は [[06_output/2026-07]]。
- **6d328a3b (habi-bff, 2026-06-16 → 07-03 の 3 週間セッション、実質 07-02→07-03 部分を取り込み)** — 6/16 起点で `add-quality-always-on` 系の議論・実装が長期継続していた大セッションが 07-02 夜〜 07-03 昼で決着。今回取り込んだ substantive は 07-03 分の **Step 4.3 `comparison_mode` の pipeline 制御へのマッピング実装 + 6.3 A/B パネル (PM Chat UI) + dev デプロイ実機動作確認**:
  1. **Step 4.3 完了**: `src/services/comparison-mode.ts` 新規、canonical `ComparisonMode` (5 モード: `full_habi`/`habi_core_off`/`direction・review・summarize_llm_off`/`all_habi_flow_off`/`baseline_llm`) を **pipeline 制御 (skipHabiCore / processingFlow / 各 Step LLM) にマッピング**。**Safety (Guard/boundary) は全モードで常時 ON**。validation → 非同期経路 (chat-start/queue/chat-processor) → `executeChatPipeline` に貫通、既定 `full_habi` = 現状不変。unit 249 green (pre-existing warning 1)。
  2. **Step 6.3 完了**: PM Chat UI に **A/B 比較パネル** (プリセット: Habi vs 素の LLM / Review ON-OFF / Core ON-OFF / Summarize ON-OFF)。同一入力を 2 モードで並列実行し左右並置、Step Debug 折りたたみ、公平性行 (`same_input`/`state`/`model`)、`run_id`。A/B プローブは会話履歴を汚さない設計。
  3. **dev デプロイ実機動作確認**: `/pm/chat` を Web で開いて **ログイン → 実行 → Step Debug (canonical order: `direction→attunement_selector→drafting→review→bff_guard→output→summarize`) → A/B 比較** の 3 段まで通過。**「器が Web で成立」= PM 側 Lv1 到達点** を実物で確認。
  4. **AWS プロファイル取り違い解消 (最大の発見)**: dev デプロイが `hlab-prod-MFA-required-policy` の explicit Deny でブロック → user 直感「`crew-user_hlab_device` が違う気がする」を受けて `~/.aws/config` を精査 → **default (`CREW-USER`) = アカウント 864310138286 (本番系・MFA 必須)** と **`bff-lambda-dev` = アカウント 062759670692 / user tatoflam / MFA 不要 / `s3://habi-bff-deploy-dev` 既存アクセス可** の別アカウントだったことが判明。**dev 用の正解プロファイルは `bff-lambda-dev`**、default では本番系にぶつかっていた。以後 dev は `AWS_PROFILE=bff-lambda-dev` を使う。詳細は [[05_learn/aws-profile-account-hlab-mfa]]。
  5. **`scripts/deploy.sh` バケット名バグ修正 (commit `f3924dc`)**: `habi-bff-deploy-${ENVIRONMENT}` を展開すると `-development`/`-production` になり samconfig (`-dev`/`-prod`) と不一致、`STACK_NAME` の `cut -c1-4` も staging を `stag` に切り詰めるバグ持ち。環境短縮名マップ (`development→dev`/`staging→staging`/`production→prod`) を導入して `S3_BUCKET` と `STACK_NAME` の両方に適用、3 環境 samconfig 完全一致確認。
  6. **`NarrativeMemoryTable` 新規作成** (DynamoDB、CloudFormation `UPDATE_COMPLETE` で作成)、全 Lambda 更新。stack `habi-bff-dev` / ap-northeast-1、API `https://ww6knpkhu4.execute-api.ap-northeast-1.amazonaws.com/development/`。
  7. **ドキュメント 3 点作成 (すべて `docs/plans/` 配下 = git 除外・機微・ローカルのみ)**: (a) `HABI_DEVELOPMENT.md` §3「現在地」を 07-03 に更新 + §9「環境・デプロイ」新設 + CHANGELOG エントリ、(b) PM 向け実装状況レポート HTML `CTO_STATUS_REPORT_260703.html` (結論 3 行 / 到達点 / 実装状況ステップ表 / Web で試す手順 / Founder が今できること / Founder 依頼 (Gate 1・Seed)) — ブラウザで開ける、(c) 先方 AI (マスター 260618 取りまとめ元) への引き渡し資料リスト `HANDOVER_TO_FOUNDER_AI_260703.md` (A: 当方→先方 / B: 先方→当方 / C: 実機 URL / D: 渡し方推奨手順 + 秘匿方針明記 = Holdout は渡さない、openspec は生 Policy 非含有なので共有可)。**tracked 変更なしのためコミット対象なし**。
  - 反映: [[03_work/habi-bff]] 07-03 H2 追加、[[05_learn/aws-profile-account-hlab-mfa]] 新規、[[06_output/2026-07]] に dev deploy URL (Founder 共有予定の実機) を追加、CTO_STATUS_REPORT / HANDOVER 2 doc は `docs/plans/` 内につき 06_output には URL のみ (成果物本体は非共有・ローカル参照のみ)。
- **a1f18d16 (v, 2026-07-03 01:27 → 01:37 JST)** — run-117 の /wiki-ingest 実行 session そのもの (final message: 「Ingest complete. Vault committed as `ebf2615` (not pushed).」)。SessionEnd 遅延 enqueue で run-118 まで残った。diary に meta-ack のみ。
- **0b8942be (v, 2026-07-03 09:30 JST)** — /wiki-ingest 再起動 → キュー空・inbox 空で「Nothing to ingest」で即終了 (`--- 775 lines total, 0 with processed:false ---`)。空振り noop。diary に meta-ack のみ。
- **6a65883e (minami-gakudo-tshirt, 2026-07-03 22:18 enqueue)** — 対応 transcript `-Users-tato-repo-github-tatoflam-minami-gakudo-tshirt/6a65883e-…jsonl` が **存在せず** (`ls | grep 6a65` 空)。SessionEnd hook が enqueue した直後に transcript が消失/移動した可能性。`hook-errors.log` に `skip=missing_transcript` で記録、キューは `processed: true` に flip して落とす (retry 手段がないため)。

### AWS プロファイル取り違いの構造メモ (Founder 級の再発防止)

CREW-USER (`864310138286`) と `bff-lambda-dev` (`062759670692`) は名前は「dev」「crew-user」だが **別 AWS アカウント**。SSO ではなく素の IAM ユーザーで、`~/.aws/credentials` に長期キーが 2 本並んでいる状態。dev バケット/スタックは 062759670692 側にしか存在せず、CREW-USER のまま `sam deploy` を叩くと **意図せず本番系 (MFA 必須ポリシー) にぶつかって Deny** → 表面的には「MFA 忘れ」に見えるが、実は「アカウント違い」。対策: (a) デプロイスクリプト冒頭で `sts get-caller-identity` の Account/User を **期待値と比較して不一致で即 abort**、(b) samconfig の `profile` フィールドを明示、(c) デプロイ用 IAM は別アカウント側のロールで発行するのが本筋 (今回は暫定的に `bff-lambda-dev` の長期キー流用)。詳細は [[05_learn/aws-profile-account-hlab-mfa]]。

### chronic defer 状態 (run-118 時点)

前回と同じ vault 側 dirty 4 ファイルに変化なし:

- `03_work/todobot.md` — ToDoBot ×8 セッションを block (`e594cbdd` ×2, `87c832cd`, `6c36b23b`, `74ef607c`, `b2998a84`, `41a0fb6a`, `e4dd720d`) = 50 日目
- `04_life/minami-gakudo-fubokai-2026.md` + `06_output/2026-05.md` — gakudo-assingment ×2 セッション (`e19b11f3`, `b7a41f9f`) を block = 50 日目
- `02_diary/2026-05-23.md` — 過去日 dirty、defer 対象外だが未 commit

計 **10 セッション chronic defer** (今回追加分なし)。「まとめて刻む」機会待ち継続。

see also: [[03_work/habi-bff]] / [[05_learn/aws-profile-account-hlab-mfa]] / [[06_output/2026-07]]
