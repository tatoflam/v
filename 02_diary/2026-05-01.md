---
title: 2026-05-01
category: 02_diary
tags: [diary]
sources: [c2dd2c85-7cc6-45d2-8df6-ebd5f5358bc4, 0d77b63d-cfe8-42b9-9e17-1b24b76b40a8]
updated: 2026-05-01
---

# 2026-05-01

## habi-bff — HABI 全体ロードマップ再構成 + Attunement Policy §1+§2+§3.2 実装（c2dd2c85、4-29 12:20 JST → 4-30 13:03 JST、約 25h スパン）

- session: c2dd2c85（cwd: habi-bff、message=30、enqueued 2026-04-30T21:45Z）
- 大きく **2 フェーズ**：(A) M1 完了時マトリクスから `HABI_DEVELOPMENT_v2.md` ＋ 4 OpenSpec 提案を作成、(B) 1 提案目 `add-attunement-policy` を実装着手
- **(A) ロードマップ再構成**：
  - 顧客側 `★★★（M1完了時）ハビバージョン別機能マトリクス260217.xlsx` シート `260216` を読み込み（バージョン v1.0/v2.0/v3.0 × 7 層 × Month 配置）。**v1（HABI_DEVELOPMENT.md）に未掲載の 4 層**（Attunement Policy / BFF Guard / 品質層・在り方テスト常時 / BFF Policy／コスト・遅延ガード）を発見
  - [docs/plans/HABI_DEVELOPMENT_v2.md](https://github.com/hlab-it-sys/habi-bff/blob/main/docs/plans/HABI_DEVELOPMENT_v2.md) を新規作成（v1 をマージ、4 層を追加、M3 以降の未起票一覧を §6 に明記）
  - 4 OpenSpec 提案を **同時生成**（`add-attunement-policy` 21 tasks / `add-bff-guard` 29 tasks / `add-quality-always-on` 26 tasks / `add-bff-policy-monitoring` 30 tasks）。すべて `proposal.md` / `design.md` / `tasks.md` / `specs/<capability>/spec.md` の 4 ファイル構成（既存 `add-user-os-persistence` と同形式）、`openspec validate --strict` 全通過
  - **適用順（依存解析）**：`attunement-policy` → `bff-guard` → `bff-policy-monitoring` → `quality-always-on`（理由：`AttunementLevel` 型と `LEVEL_ORDER` を他 3 つが消費／DOWNGRADE が `LEVEL_ORDER` 依存／メトリクス層は土台が揃ってから／Quality の Seed `allowed_attunement_levels` assert は型必須）
- **副題：CLAUDE.md と README.md の棲み分け Q&A**：README = 人間向け取扱説明書（クイックスタート・コマンド表・スクショ可）／CLAUDE.md = AI 向け作業前ブリーフィング（規約・タブー・読むべきソースのルーティング）。**両者重複禁止**、CLAUDE.md → README へ `> 詳細: README.md` で委譲、逆方向の委譲はしない（人間は CLAUDE.md を読まない前提）。`docs/README.md` は中間に位置する索引兼責務分担定義書（人間と AI の両方が参照）
- **(B) `add-attunement-policy` 実装**（11/24 tasks 完了）：
  - **§1 型と純関数（4 task）**：[src/services/attunement/types.ts](https://github.com/hlab-it-sys/habi-bff/blob/main/src/services/attunement/types.ts)（`AttunementLevel` / `SelectorInput` / `SessionState` / `AttunementDecision`）, [rules.ts](https://github.com/hlab-it-sys/habi-bff/blob/main/src/services/attunement/rules.ts)（`LEVEL_ORDER` / `isUpwardJump` / `moveDownOrHold` / `lowerByOne`）, [selector.ts](https://github.com/hlab-it-sys/habi-bff/blob/main/src/services/attunement/selector.ts)（`AttunementSelector.select` ルールベース v1.0 ＋ singleton）
  - **§4.1/§4.2 unit テスト**：5×5 真理値表 ＋ MOVE_DOWN_OR_HOLD invariant ＋ proposed 各分岐 ＋ 低信頼度 ＋ 決定性 ＋ singleton（19 件追加 → 9 suites / 125 tests）
  - **§2 配線方式の選択（Option C 短期 B / 中期 A）**：habi-os の `DraftingInput` を `node_modules/@hlab-it-sys/habi-os/dist/processing-flow/drafting-service.d.ts` で確認 → **`interventionLevel` 専用フィールドが無い**。habi-os 側 PR を待たずに進めるため、habi-bff 単独で `systemPrompt` に `[INTERVENTION_LEVEL] {level}` タグを注入する方式を選択（既存の `[DIRECTION]` `[TONE]` `[STRUCTURE]` `[BOUNDARY CAUTION]` と同じタグ書式 = spec の "structured input as a marker" と解釈可）
  - **§2 + §3.2 配線実装**：[habi-client.ts:301-340](https://github.com/hlab-it-sys/habi-bff/blob/main/src/services/habi-client.ts#L301-L340) で Direction → Selector → tag-injected `draftRequest` → Drafting の流れを構築。`ExtendedChatResponse.metadata.attunement = { chosen_level, proposed_level, jump_blocked, reason }` を露出。pino で `attunement.{chosen_level, proposed_level, jump_blocked}` を構造化フィールドとして記録
  - **§4.3 配線テスト**：tests/unit/services/habi/processing-flow.test.ts に `Attunement Selector wiring` describe を追加（注入確認／boundary flag → mirror／detailed depth で clamp & jump_blocked／metadata 露出 = 4 件追加 → 9 suites / 129 tests）
  - **検証**：`tsc --noEmit` clean ／ ESLint 新規 0 warn ／ `openspec validate add-attunement-policy --strict` valid
- **残ブロッカー（spec の Open Questions と一致）**：§3.1（StepTrace に `attunementInjection` を追加）／§3.3（同 Quality 側参照）／§4.4（StepTrace の e2e テスト）は **habi-os 側で `StepTrace` 型を拡張する PR が必要**。[`tasks.md` §8](https://github.com/hlab-it-sys/habi-bff/blob/main/openspec/changes/add-attunement-policy/tasks.md) に follow-up note 済み
- 主な学び（再利用パターン）：
  - **OpenSpec の "structured input" は typed field でなくとも、既存タグ書式に倣ったマーカーで満たせる**（spec の語彙の柔軟性）。habi-os 側の API 変更を待たずに spec 適合を保てる
  - **複数 change を同時に生成しても依存ループを避けられる順序付けが可能**（型 → ロジック → メトリクス → 品質テスト）。M2 進行を止めずに M3 以降の未起票分は遅延起票で OK
- 詳細: [[03_work/habi-bff]]
- see also: [[02_diary/2026-04-30]]（前日：SpecDrawing マラソンと前回 ingest）

## /wiki-ingest（手動・habi-bff cwd：substantive 1 + meta-flip 1、deferred 6 連続 15 回目）
- session: 本セッション（手動 invoke、cwd=habi-bff）
- 開始時 queue 11 行 unprocessed（unique 8）
- **substantive 1**: c2dd2c85（cursor 0 → EOF = +1.22 MB の新規分）— 上記「habi-bff — HABI 全体ロードマップ再構成 + Attunement Policy §1+§2+§3.2 実装」エントリで本 diary に統合、新規ページ [[03_work/habi-bff]] を作成。**4-30 のロードマップ作業を翌朝 5-1 の ingest で取り込む**形（セッション末タイムスタンプは 4-30 13:03 JST、SessionEnd hook 発火が 4-30 21:45Z = 5-1 06:45 JST）
- **flip 1 unique（1 queue 行、meta-only）**：0d77b63d（4-30 SpecDrawing cwd /wiki-ingest、commit `c865433` の実行セッション = 前 ingest run の本体）— frontmatter sources に追記（[[02_diary/2026-04-30]] / [[05_learn/specdrawing-material-presenter]]）、cursor を EOF へ
- **deferred 6 unique（dirty-target gate 連続 15 回目）= 9 queue 行**: 顔ぶれ完全に同じ — 84dd308c → `05_learn/woodone-pboard-architecture.md` (??), 832016a3 / c08d4720×2 / f75e4e61 / 385eff5c×2 → `04_life/sayama-land-contract.md` (M), d030ba7d×2 → `04_life/sayama-land-contract.md` (M) + `04_life/saltmoon-llc-operations.md` (??)
- 結論: WIP 3 ファイル（sayama 契約 / saltmoon LLC 運営 / woodone 構造）が 15 営業日連続未 commit、もはやユーザの「Obsidian で温存中の下書き」状態。本ランは habi-bff の substantive を独立クリーン取り込み（dirty 領域に触れず）でき、引き続き deferred 6 とは独立に進捗
- see also: [[05_learn/wiki-automation-pipeline]]
