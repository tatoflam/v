---
session: 146261a9-6ab0-4ad6-a9f7-95abd8da9b5e
captured: 2026-07-24T00:00:00Z
target: 03_work/meguru-pm-report
category: 03_work
tags: [project:meguru-pm-report, client:meguru, tech:python, tech:claude-code, tech:openspec, stage:active]
confidence: high
---

# triage ルーティング精度改善の完走（7/17 publish 復旧 → improve-triage-routing-accuracy → pm-status 拡張）

長期セッション（2026-07-17 10:01 JST 開始 〜 2026-07-22 12:00 JST 終了、cwd: MeguruPMReport）。
`/pm-status` での異常検知から始まり、publish 復旧・pipeline バグ修正・triage 刷新の
OpenSpec change 完走・運用可視化の拡張まで一続きに進めた回。

## 1. 7/17 publish 欠落の調査と復旧

- `/pm-status` で **[anchor] の差分基準が 2026-07-08 に固着**しているのを検知。
  アンカーは水曜（7/15）の生成+publish 成功時に rotate する設計なので、
  7/15 の publish が失敗/スキップされていたことが判明
- あわせてユーザー指摘: published Google ドキュメントから **7/16 分以降、議事録
  （7/15 記載内容）が消えていた** → 7/17 レポートを議事録復元版として再生成・再 publish
- daily_pipeline の再実行を Monitor で監視しながら段階的に復旧
  （wiki-refresh の WARN は非致命で続行）

## 2. pipeline バグ 2 件の修正（commit `8539c91`）

- **Step 0a**: GmailGrabber の raw query に日付フィルタ（after:）が埋め込まれず
  全履歴取得で暴走するバグ → 日付フィルタを query に埋め込む修正
- **Step 0b**: ingest の .eml ループ化修正
- 暴走の後始末: `~/meguru-data/mail/.trash-wiki_refresh_2026-07-17-runaway`
  （**11GB** の退避ディレクトリ）はユーザーが手動削除
- push 時、publish の worktree 経由 payload commit（`db02c02`）へ fast-forward
  追従してから積む運用を確認

## 3. OpenSpec change `improve-triage-routing-accuracy` 実装 + archive

実体は Plugins-Meguru の business-curator（MeguruPMReport 13 ファイル +
Plugins-Meguru 25+ ファイル）。設計の骨子:

- **増分 triage**: 既決 entry をスキップ。全量再走査は `--full` 明示時のみ
  （本来仕様は 2 日 window の日次実行、理想は 1 日 window）
- **統計 3 段**を LLM の前段に追加: thread 継承 / 参加者共起 / テキスト類似
  （routing memory 由来。`--rebuild` で rule_match 実績から再構築可能）
- **LLM フォールバックのモデルを sonnet に固定**（`wiki_triage_model: sonnet`）。
  従来は `--model` 未指定で CLI デフォルトの最上位モデルに課金されていた。
  上限 `wiki_triage_llm_limit: 200` call/日、超過分は `llm_limit_reached` で
  翌日に持ち越し（失われない）
- §0 にコスト封じ込めを明記してから実装 → `/opsx:archive` で specs 確定反映
- 7/21 にフルセット実行で実データ検証: LLM フォールバック 166 件新規確定、
  週次レポート 2026-07-21 生成 + publish まで完走

## 4. noise ルールの手動追記手順（_alias_resolver.md）

rule mining（`state/rule_mining_2026-07-21.md`）の提案を受け、
**ToDoBot / eFax メッセージ** を noise 扱いにするルールを追加:

- 追記先: `MeguruWiki/_alias_resolver.md` の `### subject_pattern → noise`
  セクション（既存 9 ルールの並び）
- 書式: `- {正規表現} → {noise 理由ラベル}`。**矢印は全角「→」**（半角不可）、
  LHS は Python `re` 互換の正規表現で件名への部分一致（search）判定

## 5. `/pm-status` [triage] セクション拡張（commit `e8414e6`、shell テスト 16/16）

- LLM 予算の消費状況（`calls 200/200`）と**上限先送りキューの内訳**
  （上位送信元ドメイン 3 + 直近件名サンプル）を表示。データは当日の
  triage_log の `reason: "llm_limit_reached"` エントリから取得（plugin 変更不要）
- 早速の示唆: 待ち行列 4,494 件の **1/3 が社内メール meguru-construction.com
  （1,546 件）** — 日報・連絡系が多く LLM に流す価値が低い可能性。
  `i-chord.com`（インターコード定時連絡 56 件）も noise 候補
- 滞留の解消ペースは約 176〜198 件/日の漸進（計画的ドレイン中）

## Links / 関連

- 関連 change: improve-triage-routing-accuracy（archive 済み）
- 関連 diary: [[02_diary/2026-07-17]] [[02_diary/2026-07-21]] [[02_diary/2026-07-22]]
