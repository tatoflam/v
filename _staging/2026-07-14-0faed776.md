---
session: 0faed776-4d79-4b2b-a946-d34b304110da
captured: 2026-07-14T12:16:35Z
target: 03_work/meguru-pm-report
category: 03_work
tags: [project:meguru-pm-report, client:meguru, tech:python, stage:active, topic:openspec]
confidence: high
---

# Long-haul spec session: docs overhaul → fix-mbox → wiki-absorb tools → sync → archive → daily_pipeline §2 wiring

長丁場（05:45Z–12:14Z / 約 6.5h）の spec 開発セッション。「次の spec 開発を何から手をつけるか」から始まり、docs 整備・複数 change の起票/実装/archive・6 PR のマージまで到達。cwd = `meguruit/MeguruPMReport`。

## ユーザーの主要な意思決定・指示
- **アーキテクチャ選択**: 「MeguruWiki を plugin root にするか、`~/meguru-data` にデータ集約するか」を検討 → **a 案（データ集約）で確定**。決定2を先に潰す方向。
- **Weekly Report の実行頻度**: 「原則 daily pipeline が回るたびに毎回実施、報告差分は 1 週間前の報告内容からの差分に」できるか要望 → 論理的/実務的に難しければ代替提案を求める。結果、別 change **`add-weekly-report-daily-anchor-diff`**（水曜アンカー差分）を propose（0/26、未着手）。
- 生成頻度は「一旦 daily」。`state/weekly_anchor.json` は **git 追跡**。inline prompt 注入方式は詳細後で確認。
- 「2 change ぶんをまとめて commit」→ その後マージ・ブランチ削除・古いローカルブランチ掃除も指示。
- docs 整備の大タスク（fable-5 モデルで指示）: **全 Spec・実装・open change の spec を確認し、要件/設計（アーキ・機能・非機能・運用）/今後の実装を MECE に整理して README.md や docs 配下（HTML）に整備。システムコンテクスト図を mermaid で（詳細になりすぎない粒度）+ 生成画像も docs に含める。過程でリファクタ対象を見つけたら openspec で change 起票。**
- 日次パイプラインへの本音: **「定例レポートが各案件に紛れ込むのを除き、意味のあるメールは全量解析したい」** → 母集団フィルタは Step 0a の gmail-grabber `--raw-query` で適用する設計に補正。
- 「できるだけ自分で、推奨の方法でどんどん実装して」という強い委任スタンス。マージ/ブランチ削除は user が GitHub 上で実施（「github 上で両ブランチをマージ、削除したよ」）。

## 成果（このセッションの全体）
docs 一式 + システム図 → fix-mbox → absorb ツール 3 種（35 tests）→ sync ツール → archive 2 件 → daily_pipeline §2 配線 = **計 6 PR**。

### change / capability
- **`add-wiki-absorb-orchestration`**: 24/50 tasks 完了（§1 ツール + §2/§3 配線）。Path2（データ集約）方式に revise。残: §4 weekly-report の wiki ドリルダウンリンク / §5 sync を daily 配線 / §6 pm-status ダッシュボード / §7 noise ルール整備 + 1 日ピロット較正 → 有効化。
- **`align-daily-pipeline-spec-with-cli-execution`**: base spec を「6 ステップ・CLI 直呼び」に是正 → archive。
- **`fix-mbox-parser-encoding`**: 新 capability **`mail-ingest`**。get_bytes + per-message recovery で mbox ingest を hardening → archive。
- **`add-weekly-report-daily-anchor-diff`**: 0/26、水曜アンカー差分、未着手。

### daily_pipeline §2 配線
`daily_pipeline.sh` に wiki_refresh (Step 0a–0c) + Step 7 (auto-absorb) + 月曜 catch-up を追加。**全フラグ default OFF・非致命**（マージしても runtime 挙動は不変、有効化は 1 日ピロット較正後の ops 判断）。

### 検証の実践（重要な学び）
TDD/実測で **3 件の前提誤りを commit 前に是正**:
1. fix-mbox の破損仮説
2. sync ツールの入力前提
3. **`ingest_cli` に to/cc フィルタは存在しない** → 母集団フィルタは Step 0a の gmail-grabber `--raw-query` へ

## 外部公開（→ 06_output 候補、distill で 2026-07 へ）
- MeguruPMReport **PR #9**（wiki-absorb revision + weekly-anchor propose）
- MeguruPMReport **PR #11**（archive 2 件: align-daily-pipeline + fix-mbox + wiki-absorb delta 追随）
- MeguruPMReport **PR #12**（§2 daily_pipeline 配線、default OFF）
- Plugins-Meguru **PR #3 / #4**（business-curator: absorb ツール full-volume model + harden-mbox-ingest）
- 全 6 PR マージ済み。マージ順は #11 → #12（同一 delta ファイルの別ハンク、依存の向き通りが 3-way マージが素直）。
- git log 上の確定コミット: `de304af` (Merge #12) / `c259c7c` (Merge #11) / `a53126d` (§2 配線) / `afdcfe8` (archive)。

## 現在の状態（セッション終了時）
- ローカルは main のみ（済みブランチ全削除、prune 済み）。
- main に §1 ツール + §2 配線 + spec 是正が揃う。runtime 挙動は不変（wiki フラグ default false）。
- open change 残り 2 本: `add-wiki-absorb-orchestration`（継続分 26 tasks）/ `add-weekly-report-daily-anchor-diff`（0/26）。

## 次の着手候補
- §4 weekly-report の wiki ドリルダウンリンク（drive-publisher 連携）
- §7 + 有効化: noise ルール整備 → 1 日ピロット実測 → フラグ ON
- 別 change weekly-anchor（水曜アンカー差分）着手

## 補足（無関係）
IDE で `~/Downloads/php-errors.log`（contact_send.php の GAS 通信失敗 / フォーマットエラー）が選択されたが本タスクと無関係。未対応。
