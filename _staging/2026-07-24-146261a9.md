---
session: 146261a9-6ab0-4ad6-a9f7-95abd8da9b5e
captured: 2026-07-24T08:01:58Z
target: 03_work/meguru-pm-report
category: 03_work
tags: [project:meguru-pm-report, client:meguru, tech:claude-code, tech:python, tech:openspec, stage:active]
confidence: high
---

# PM pipeline 障害復旧 + improve-triage-routing-accuracy 完全実装〜archive（7/17〜7/22 の長期セッション）

cwd: `/Users/tato/repo/github/meguruit/MeguruPMReport`。3 日に跨る大型セッション（4MB transcript、テキストターンのみ抽出）。wiki triage 系の実装は Plugins-Meguru/business-curator 側、ルール正典は MeguruWiki 側。

## 2026-07-17: pipeline 暴走の発見と復旧

- `/pm-status` で「アンカーが 7/08 のまま」を検知 → 調査で 2 障害が判明:
  1. **Step 0a 暴走**: `daily_pipeline.sh` が `--after` と `--raw-query` を併用していたが、gmail-grabber は **raw_query 指定時に日付フィルタを無視する仕様** → 2 日分のつもりが 3/31 以降の全メール（6,900 通超・11GB）をダウンロード中だった（前日 wiki_refresh 有効化後の初回発火）。修正 = raw query に `after:` 句を直接埋め込み。Step 0b の `.eml` ディレクトリ非対応バグも修正
  2. **議事録消失**: 7/15 Doc の議題 1〜7 は baseline sync 経由で `reports/2026-07-15_weekly_update.md` に保全されていたが、7/16 生成が `## 議題` 節を丸ごと落として publish していた（carry-forward 対象が案件別 `- メモ:` 欄のみのため）
- 復旧: 暴走停止 → バグ修正 → 11GB 残骸退避（後日ユーザーが手動削除）→ pipeline 再実行（92 通で正常）→ 7/17 レポートに議題 1〜7 復元して publish（https://meguru-pm-report.web.app/r/2026-07-17/ + Doc）。commit `8539c91`
- アンカー 7/08 停滞の真因 = 7/15（水曜）のネットワーク断で pipeline 失敗。7/22 水曜に自動正常化見込みと診断（後日そのとおり rotate）
- 決定: 全量再 triage を停止し、暫定ガード `wiki_triage_llm: false` を設定。モデル固定（`wiki_triage_model: sonnet`）を `improve-triage-routing-accuracy` change のスコープに追加（tasks §0「コスト封じ込め」、design D10 増分 triage / D11 モデル固定）
- 仕様確認: fetch/ingest（Step 0a/0b）は「2 日 window の日次」で意図どおり、triage（Step 0c）は実装が **全件再分類** だった → §0 で「増分実行が既定」に正式化

## 2026-07-21: /opsx:apply improve-triage-routing-accuracy — 33/33 タスク完了

- **§0 コスト封じ込め**: triage 既定を増分実行に（既決 12,000+ 件スキップ、`--full` で全量）。`--model` 配線 + daily pipeline が `wiki_triage_model: sonnet` を渡す。暫定ガード解除
- **§1 routing memory**: `~/meguru-data/state/routing_memory/`（thread 索引 / 参加者共起 / セントロイド）。増分更新・entry id 冪等・破損時空メモリ続行・`--rebuild`。実データ 6,424 件から初期構築
- **§2 統計 3 段**: 件名正規化（全角/漢数字丁目/接尾語）+ thread 継承・参加者共起・テキスト類似を LLM 前段に挿入
- **§3 較正（holdout 6,424 件）**: participant_affinity precision 1.000 / coverage 6.2%、text_similarity precision 0.985 / 5.2% の閾値確定。thread 継承の見かけ precision 62% は全件 project↔vendor の facet 反転で案件間誤りゼロと判明
- **§4 rule mining**: `tools/rule_mining.py`（noise/domain/エイリアス漏れ/新規の候補提案のみ、正典自動書き込み禁止を guard test で担保）。第 1 サイクルで 39 提案（eFax 160 件・ToDoBot 27 件の noise 候補など）
- **§5 統合**: Step 0d（週次ゲート・既定 OFF）+ pm_status `[triage]` 行刷新
- **§6 実運用検証**: 増分 triage で滞留 5,400 件中 489 件新規確定（正規化だけで 342 件救済）。business-curator pytest 799 passed
- **追加で発覚・修正した障害 3 件**:
  1. 7/18〜7/21 朝の pipeline 4 連続失敗の真因 = 個人 wiki の SessionEnd hook が 7/18 の attended ingest で pause されたまま → 全 `claude -p` が失敗。hook 復元 + triage subprocess に `WIKI_INGEST_WORKER=1` ガード追加
  2. `_cfg` の yq `//` バグで `wiki_triage_llm: false` が default `true` に化けていた（7/17 の暫定ガードは実は無効、7/18 朝に未固定モデルで ~50 分誤走行）→ 恒久修正
  3. `--llm-limit` 追加（`wiki_triage_llm_limit: 200`）: バックログを一晩で全処理しない予算キャップ
- フルセット実行（新体制初本番): LLM フォールバック 166 件新規確定、absorb が **96 shard / 620 エントリ** を wiki に吸収、`NEW_PROJECT:大田区仲池上2丁目`・`中野区南台1丁目`・`新宿区弁天町` など LLM 発見の新規案件 shard 自動生成。7/21 レポートは議題を今回も生成が落としたため 7/17 Doc 版から復元して republish（手動復元 2 回目 → 「`## 議題` 節の carry-forward」change 起票候補）

## 2026-07-22: archive + noise ルール + インデックス表示名 + pm-status 拡張

- `/opsx:archive improve-triage-routing-accuracy`: specs へ +17 Requirements / ~1 modified（`routing-memory`・`routing-rule-mining` 新 capability、`triage-classifier` 8 段 chain 化、`daily-pipeline` 3 件追加）。commit `52a33d9`
- 今朝 9:00 の無人実行（新体制初回）正常: 7/22 レポート配信、**アンカーが 7/22 に正常 rotate**（7/15 断以来初）、triage 増分 234 件処理（LLM 164 / stat 16 / thread 2 / noise 38、unclassified −198）
- **noise ルール追加**（ユーザーが `_alias_resolver.md` を編集、こちらで検証+commit `e644250`）: `### subject_pattern → noise` に `ToDoBot` / `eFax メッセージ` の 2 行。書式 = `- {Python re 正規表現} → {理由ラベル}`、全角「→」必須、件名部分一致（search）。noise は 8 段 chain の段 1（最優先）で shards 分類にも LLM にも入らない
- **clients/vendors インデックス表示名**: `/wiki-index-rebuild` がリンク表示に canonical（ドメイン/メルアド）をそのまま使っていた → shard ページ H1（`# take-office.co.jp（テイクオフィス株式会社）` 形式）から名称を取り出す `ShardTitleResolver` を実装（表示専用、triage マッチング無影響。H1 に和名が無いページはドメイン表示に退化 = ページ側 H1 を書けば次回 rebuild で自動改善）。Plugins-Meguru `dc1b53a` / MeguruWiki `b5141b2`
- **pm-status `[triage]` に LLM 上限可視化**（commit `e8414e6`）: `calls 200/200`（予算消費）、上限先送り件数、待ち行列上位ドメイン + 件名サンプル。初出力で **待ち行列の 1/3 が meguru-construction.com（社内メール 1,546 件）** と判明 — 社内定型系の noise/category ルール追加が LLM キュー削減に最も効くという示唆。`i-chord.com`（インターコード定時連絡）も noise 候補のまま

## 経過観察ポイント（セッション終了時点）

- unclassified 残 ~4,300〜4,494 件を日次 ~200 件（LLM 上限）で漸進消化中、完済まで ~3 週間ペース
- pending_weekly_absorb backlog 20 件（WARN 閾値 10 超、増加継続なら noise ルール不足のサイン）

関連: [[03_work/meguru-wiki]] [[03_work/businesscurator]]
