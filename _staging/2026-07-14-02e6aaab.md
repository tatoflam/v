---
session: 02e6aaab-9fe3-4c4c-aa19-9bf404d1413e
captured: 2026-07-14T05:47:00Z
target: 03_work/meguru-pm-report
category: 03_work
tags: [project:meguru-pm-report, client:meguru, tech:python, tech:openspec, stage:done, topic:llm-triage]
confidence: high
---

# add-triage-routing-enrichment — 設計→実装→実データ§7検証→3リポPRマージ→archive まで完結

MeguruPMReport の未検証 OpenSpec change を片付けたセッション。cwd=MeguruPMReport だが実装対象の大半は sibling repo `meguruit/Plugins-Meguru/business-curator` と `meguruit/MeguruWiki`（`plugins/*` は Plugins-Meguru 作業ツリーへの symlink）。
※ transcript は 592 turns（本ダイジェストは text ブロック全走査で作成、tool 実行ノイズは除外）。

## 出発点
- 未実装・未検証の change は 2 件: **add-triage-routing-enrichment（0/39）** と **add-wiki-absorb-orchestration（0/45）**。依存の向きは前者 → 後者なので前者から着手（唯一の正しい順序）。
- 方針: コード+テスト（§1–§6, §8）を先に仕上げ、§7 実データ再 triage（venv + `claude -p` + 実メール多数が必要な長時間・データ依存の検証）は分離 —— ユーザの運用方針「長時間処理は apply 中に回さず次の自然実行で検証」に従う。

## 実装した内容（business-curator, venv 作成済）
5 段 triage パイプライン **Noise → Domain → Category → Alias → LLM** を新規構築（既存 `TriageOrchestrator` は後方互換で温存し `FivePhaseTriagePipeline` を別立て）。
- 新規モジュール: `domain/types/routing.py`, `infrastructure/repositories/routing_table_repository.py`（`## routing/` パーサ、後方互換）, `application/triage/{noise_filter,domain_router,pipeline,llm_fallback}.py`
- **`llm_fallback.py`**: alias 全載せ + 候補 pre-rank + slug 強制 + 1 回リトライ、`primary_slug=None` を禁止戻り値化
- **`shared/alias_resolver.py`**: `resolve_slug` を `resolve_slug_candidates` に一般化（ランキング exact > 前方一致 > 部分一致 の 3 tier）。migration 側も候補列挙化（§5.7）。副作用: `東尾久4丁目 ⊂ 東尾久` が `partial`→`prefix` に再分類され既存テストの期待値を更新
- `MeguruWiki/_alias_resolver.md` に **`## routing/` セクション投入: domain 28 / noise 6 / category 3**（実 vendor/client shard 由来の slug・正規表現、alias リーク無し）
- テスト: business-curator **673 passed**、pm-master-grabber 137 passed、`openspec validate --strict` valid、ruff clean。既存 2 failed は無関係な `jooto-absorb` 構造テストの未更新（baseline から存在、触らず）

## 意図的に保留した点（理由あり）
- **§5.6**（AliasMatcher の exact-only 化）: 現 rule_engine は 4 shard 種の部分一致、`resolve_slug_candidates` は projects 限定の名前一致。単純置換すると clients/vendors/knowledge の被覆が落ちる。§7 実データ before/after 検証なしに段 3 の意味を変えるのは危険 → 分離。**後に実測で substring が有効と確認し据え置き決定**。
- §7.1/7.3, §6.4 も非ブロッカーとして archive 時まで繰延（計 7 タスク繰延）。

## 重要な発見・訂正（§7 の実態）
当初「6時 wikiMaintenance triage cron が自然に §7 を実行する」という説明は**誤り**と判明:
1. その cron は本マシンに **未登録**（`launchctl` にあるのは `com.meguru.pm.daily` のみ）
2. meguruit 環境のメールデータ **0 件**（raw-entries も eml も 0、旧 Bizuayeu 1,743 通は未移行）
3. wikiMaintenance の `_env.sh` が **削除済み旧 Bizuayeu プラグインを参照**したまま（consolidate 保留タスク 3.3 の repoint 未実施）

→ 代わりにまず**実 resolver + 実 shard 相手のライブ・スモーク**を実施（7 ケース: noise 2 / rule_match 4 [domain/category/alias 経路すべて発火] / unclassified 1）。5 段すべて正しく発火することを確証。

## 本物の §7 検証を実行
`_env.sh` を repoint（`B_CURATOR_DIR`→Plugins-Meguru/business-curator, PYTHON→venv）し、Gmail ingest（getGmail → ingest）を実行 → **実メール 5,716 通を取り込み再 triage、noise 2→112**。分析母集団を確定: **togami-log@ ∪ design@ 宛 = 2,249 件**。

## PII データ整理
取得した eml **5,717 件（2.5GB）を `/tmp`（macOS 揮発領域）から `~/meguru-data/mail/thomma/` へ恒久移動**。二重に git-safe を検証済み: `~/meguru-data` は git リポ外 + 到達経路 `MeguruPMReport/data` symlink も `.gitignore:/data` 除外 + git は symlink 先を追跡しない → commit されることは絶対にない。壊れた mbox（iso2022_jp 破損 1 通）は除去。raw-entries（5,716、MeguruWiki gitignored）は据え置きで再 triage/absorb は eml 無しで随時可能。

## 公開・成果物（06_output candidate — distill が振り分け）
- 実装 3 PR **マージ済**: [Plugins-Meguru#2](https://github.com/meguruit/Plugins-Meguru/pull/2)（6ef5c21）/ [MeguruWiki#2](https://github.com/meguruit/MeguruWiki/pull/2)（dfca77d）/ [MeguruPMReport#7](https://github.com/meguruit/MeguruPMReport/pull/7)（b657e97）
- **`openspec archive add-triage-routing-enrichment -y`** 実行 → 新 capability **`triage-classifier`（6 requirements）** を `openspec/specs/` に確定、change を `archive/2026-07-14-add-triage-routing-enrichment/` へ移動。`openspec validate --specs` = **15/15 passed**
- archive PR: [meguruit/MeguruPMReport#8](https://github.com/meguruit/MeguruPMReport/pull/8)

## 決定事項
- 2026-07-14: slug-aware LLM fallback で `primary_slug=None` を禁止戻り値化 — GAP 案件でも必ず候補 slug を返させるため
- 2026-07-14: §5.6 AliasMatcher exact-only 化は**据え置き** — 実測で substring 一致が clients/vendors/knowledge の被覆に有効と確認
- 2026-07-14: PII eml は `~/meguru-data/mail/thomma/` に恒久保持（`/tmp` は再起動で揮発、削除より移動）

## 次の一手（別セッション可）
- archive PR#8 マージで openspec 上も完全クローズ
- **add-wiki-absorb-orchestration（0/45）** 本設計 — §7 で確定した設計入力（togami-log@ ∪ design@ 宛フィルタ、~/meguru-data eml 温存、LLM フォールバック段は実装済）を土台に GAP 案件 absorb へ

## diary
14:44  add-triage-routing-enrichment を実装→実データ§7検証→3リポPRマージ→archive まで完結
